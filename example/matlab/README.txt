Calls from Matlab to YARP pass through Java, so you should first compile the Java interface.
At the moment this is in /example/swig but one day it will be moved to a better location.

Contains:
show-images.m
-nat

