add_py_callback_example {#yarp_3_4}
-----------

### Bindings

#### Python

* Added `example_callback.py`, which showcases a `BufferedPort` with an attached
  callback handler for logging incoming bottles.
