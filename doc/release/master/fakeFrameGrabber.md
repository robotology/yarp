fakeFrameGrabber {#master}
----------------

### Devices

* The `test_grabber` was renamed `fakeFrameGrabber`. The old name is kept for
  compatibility, but it will be deprecated and removed in a future release.
