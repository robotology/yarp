fix_mas_remapper_option_validation {#yarp_4_0}
----------------------------------

* `multipleanalogsensorsremapper` now rejects unknown sensor-selection options
  ending in `Names` instead of silently ignoring them.
