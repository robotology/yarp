set(YARP_MANAGER_VERSION @YARP_VERSION@)

@PACKAGE_INIT@

set_and_check(YARP_MANAGER_INCLUDEDIR @PACKAGE_YARP_MANAGER_INCLUDEDIR@)

if(NOT TARGET YARP_manager)
    include(${CMAKE_CURRENT_LIST_DIR}/YARPManagerTargets.cmake)
endif()

# Compatibility
set(YARP_MANAGER_LIBRARIES YARP_manager)
set(YARP_MANAGER_INCLUDE_DIRS ${YARP_MANAGER_INCLUDEDIR})
