/*
 * SPDX-FileCopyrightText: 2006-2021 Istituto Italiano di Tecnologia (IIT)
 * SPDX-License-Identifier: BSD-3-Clause
 */

// Autogenerated by Thrift Compiler (0.14.1-yarped)
//
// This is an automatically generated file.
// It could get re-generated if the ALLOW_IDL_GENERATION flag is on.

#include <yarp/os/SystemInfoData.h>

namespace yarp::os {

// Constructor with field values
SystemInfoData::SystemInfoData(const MemoryInfoData& memory,
                               const StorageInfoData& storage,
                               const ProcessorInfoData& processor,
                               const LoadInfoData& load,
                               const PlatformInfoData& platform,
                               const UserInfoData& user,
                               const ProcessInfoData& process) :
        WirePortable(),
        memory(memory),
        storage(storage),
        processor(processor),
        load(load),
        platform(platform),
        user(user),
        process(process)
{
}

// Read structure on a Wire
bool SystemInfoData::read(yarp::os::idl::WireReader& reader)
{
    if (!read_memory(reader)) {
        return false;
    }
    if (!read_storage(reader)) {
        return false;
    }
    if (!read_processor(reader)) {
        return false;
    }
    if (!read_load(reader)) {
        return false;
    }
    if (!read_platform(reader)) {
        return false;
    }
    if (!read_user(reader)) {
        return false;
    }
    if (!read_process(reader)) {
        return false;
    }
    if (reader.isError()) {
        return false;
    }
    return true;
}

// Read structure on a Connection
bool SystemInfoData::read(yarp::os::ConnectionReader& connection)
{
    yarp::os::idl::WireReader reader(connection);
    if (!reader.readListHeader(31)) {
        return false;
    }
    if (!read(reader)) {
        return false;
    }
    return true;
}

// Write structure on a Wire
bool SystemInfoData::write(const yarp::os::idl::WireWriter& writer) const
{
    if (!write_memory(writer)) {
        return false;
    }
    if (!write_storage(writer)) {
        return false;
    }
    if (!write_processor(writer)) {
        return false;
    }
    if (!write_load(writer)) {
        return false;
    }
    if (!write_platform(writer)) {
        return false;
    }
    if (!write_user(writer)) {
        return false;
    }
    if (!write_process(writer)) {
        return false;
    }
    if (writer.isError()) {
        return false;
    }
    return true;
}

// Write structure on a Connection
bool SystemInfoData::write(yarp::os::ConnectionWriter& connection) const
{
    yarp::os::idl::WireWriter writer(connection);
    if (!writer.writeListHeader(31)) {
        return false;
    }
    if (!write(writer)) {
        return false;
    }
    return true;
}

// Convert to a printable string
std::string SystemInfoData::toString() const
{
    yarp::os::Bottle b;
    if (!yarp::os::Portable::copyPortable(*this, b)) {
        return {};
    }
    return b.toString();
}

// read memory field
bool SystemInfoData::read_memory(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.read(memory)) {
        reader.fail();
        return false;
    }
    return true;
}

// write memory field
bool SystemInfoData::write_memory(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.write(memory)) {
        return false;
    }
    return true;
}

// read (nested) memory field
bool SystemInfoData::nested_read_memory(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readNested(memory)) {
        reader.fail();
        return false;
    }
    return true;
}

// write (nested) memory field
bool SystemInfoData::nested_write_memory(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeNested(memory)) {
        return false;
    }
    return true;
}

// read storage field
bool SystemInfoData::read_storage(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.read(storage)) {
        reader.fail();
        return false;
    }
    return true;
}

// write storage field
bool SystemInfoData::write_storage(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.write(storage)) {
        return false;
    }
    return true;
}

// read (nested) storage field
bool SystemInfoData::nested_read_storage(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readNested(storage)) {
        reader.fail();
        return false;
    }
    return true;
}

// write (nested) storage field
bool SystemInfoData::nested_write_storage(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeNested(storage)) {
        return false;
    }
    return true;
}

// read processor field
bool SystemInfoData::read_processor(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.read(processor)) {
        reader.fail();
        return false;
    }
    return true;
}

// write processor field
bool SystemInfoData::write_processor(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.write(processor)) {
        return false;
    }
    return true;
}

// read (nested) processor field
bool SystemInfoData::nested_read_processor(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readNested(processor)) {
        reader.fail();
        return false;
    }
    return true;
}

// write (nested) processor field
bool SystemInfoData::nested_write_processor(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeNested(processor)) {
        return false;
    }
    return true;
}

// read load field
bool SystemInfoData::read_load(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.read(load)) {
        reader.fail();
        return false;
    }
    return true;
}

// write load field
bool SystemInfoData::write_load(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.write(load)) {
        return false;
    }
    return true;
}

// read (nested) load field
bool SystemInfoData::nested_read_load(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readNested(load)) {
        reader.fail();
        return false;
    }
    return true;
}

// write (nested) load field
bool SystemInfoData::nested_write_load(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeNested(load)) {
        return false;
    }
    return true;
}

// read platform field
bool SystemInfoData::read_platform(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.read(platform)) {
        reader.fail();
        return false;
    }
    return true;
}

// write platform field
bool SystemInfoData::write_platform(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.write(platform)) {
        return false;
    }
    return true;
}

// read (nested) platform field
bool SystemInfoData::nested_read_platform(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readNested(platform)) {
        reader.fail();
        return false;
    }
    return true;
}

// write (nested) platform field
bool SystemInfoData::nested_write_platform(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeNested(platform)) {
        return false;
    }
    return true;
}

// read user field
bool SystemInfoData::read_user(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.read(user)) {
        reader.fail();
        return false;
    }
    return true;
}

// write user field
bool SystemInfoData::write_user(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.write(user)) {
        return false;
    }
    return true;
}

// read (nested) user field
bool SystemInfoData::nested_read_user(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readNested(user)) {
        reader.fail();
        return false;
    }
    return true;
}

// write (nested) user field
bool SystemInfoData::nested_write_user(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeNested(user)) {
        return false;
    }
    return true;
}

// read process field
bool SystemInfoData::read_process(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.read(process)) {
        reader.fail();
        return false;
    }
    return true;
}

// write process field
bool SystemInfoData::write_process(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.write(process)) {
        return false;
    }
    return true;
}

// read (nested) process field
bool SystemInfoData::nested_read_process(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readNested(process)) {
        reader.fail();
        return false;
    }
    return true;
}

// write (nested) process field
bool SystemInfoData::nested_write_process(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeNested(process)) {
        return false;
    }
    return true;
}

} // namespace yarp::os
