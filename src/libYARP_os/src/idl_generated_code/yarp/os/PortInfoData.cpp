/*
 * SPDX-FileCopyrightText: 2006-2021 Istituto Italiano di Tecnologia (IIT)
 * SPDX-License-Identifier: BSD-3-Clause
 */

// Autogenerated by Thrift Compiler (0.14.1-yarped)
//
// This is an automatically generated file.
// It could get re-generated if the ALLOW_IDL_GENERATION flag is on.

#include <yarp/os/PortInfoData.h>

namespace yarp::os {

// Constructor with field values
PortInfoData::PortInfoData(const bool is_input,
                           const bool is_output,
                           const bool is_rpc,
                           const std::string& type) :
        WirePortable(),
        is_input(is_input),
        is_output(is_output),
        is_rpc(is_rpc),
        type(type)
{
}

// Read structure on a Wire
bool PortInfoData::read(yarp::os::idl::WireReader& reader)
{
    if (!read_is_input(reader)) {
        return false;
    }
    if (!read_is_output(reader)) {
        return false;
    }
    if (!read_is_rpc(reader)) {
        return false;
    }
    if (!read_type(reader)) {
        return false;
    }
    if (reader.isError()) {
        return false;
    }
    return true;
}

// Read structure on a Connection
bool PortInfoData::read(yarp::os::ConnectionReader& connection)
{
    yarp::os::idl::WireReader reader(connection);
    if (!reader.readListHeader(4)) {
        return false;
    }
    if (!read(reader)) {
        return false;
    }
    return true;
}

// Write structure on a Wire
bool PortInfoData::write(const yarp::os::idl::WireWriter& writer) const
{
    if (!write_is_input(writer)) {
        return false;
    }
    if (!write_is_output(writer)) {
        return false;
    }
    if (!write_is_rpc(writer)) {
        return false;
    }
    if (!write_type(writer)) {
        return false;
    }
    if (writer.isError()) {
        return false;
    }
    return true;
}

// Write structure on a Connection
bool PortInfoData::write(yarp::os::ConnectionWriter& connection) const
{
    yarp::os::idl::WireWriter writer(connection);
    if (!writer.writeListHeader(4)) {
        return false;
    }
    if (!write(writer)) {
        return false;
    }
    return true;
}

// Convert to a printable string
std::string PortInfoData::toString() const
{
    yarp::os::Bottle b;
    if (!yarp::os::Portable::copyPortable(*this, b)) {
        return {};
    }
    return b.toString();
}

// read is_input field
bool PortInfoData::read_is_input(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readBool(is_input)) {
        reader.fail();
        return false;
    }
    return true;
}

// write is_input field
bool PortInfoData::write_is_input(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeBool(is_input)) {
        return false;
    }
    return true;
}

// read (nested) is_input field
bool PortInfoData::nested_read_is_input(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readBool(is_input)) {
        reader.fail();
        return false;
    }
    return true;
}

// write (nested) is_input field
bool PortInfoData::nested_write_is_input(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeBool(is_input)) {
        return false;
    }
    return true;
}

// read is_output field
bool PortInfoData::read_is_output(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readBool(is_output)) {
        reader.fail();
        return false;
    }
    return true;
}

// write is_output field
bool PortInfoData::write_is_output(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeBool(is_output)) {
        return false;
    }
    return true;
}

// read (nested) is_output field
bool PortInfoData::nested_read_is_output(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readBool(is_output)) {
        reader.fail();
        return false;
    }
    return true;
}

// write (nested) is_output field
bool PortInfoData::nested_write_is_output(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeBool(is_output)) {
        return false;
    }
    return true;
}

// read is_rpc field
bool PortInfoData::read_is_rpc(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readBool(is_rpc)) {
        reader.fail();
        return false;
    }
    return true;
}

// write is_rpc field
bool PortInfoData::write_is_rpc(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeBool(is_rpc)) {
        return false;
    }
    return true;
}

// read (nested) is_rpc field
bool PortInfoData::nested_read_is_rpc(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readBool(is_rpc)) {
        reader.fail();
        return false;
    }
    return true;
}

// write (nested) is_rpc field
bool PortInfoData::nested_write_is_rpc(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeBool(is_rpc)) {
        return false;
    }
    return true;
}

// read type field
bool PortInfoData::read_type(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readString(type)) {
        reader.fail();
        return false;
    }
    return true;
}

// write type field
bool PortInfoData::write_type(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeString(type)) {
        return false;
    }
    return true;
}

// read (nested) type field
bool PortInfoData::nested_read_type(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readString(type)) {
        reader.fail();
        return false;
    }
    return true;
}

// write (nested) type field
bool PortInfoData::nested_write_type(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeString(type)) {
        return false;
    }
    return true;
}

} // namespace yarp::os
