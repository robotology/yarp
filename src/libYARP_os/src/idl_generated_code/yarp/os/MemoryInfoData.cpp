/*
 * SPDX-FileCopyrightText: 2006-2021 Istituto Italiano di Tecnologia (IIT)
 * SPDX-License-Identifier: BSD-3-Clause
 */

// Autogenerated by Thrift Compiler (0.14.1-yarped)
//
// This is an automatically generated file.
// It could get re-generated if the ALLOW_IDL_GENERATION flag is on.

#include <yarp/os/MemoryInfoData.h>

namespace yarp::os {

// Constructor with field values
MemoryInfoData::MemoryInfoData(const std::int32_t totalSpace,
                               const std::int32_t freeSpace) :
        WirePortable(),
        totalSpace(totalSpace),
        freeSpace(freeSpace)
{
}

// Read structure on a Wire
bool MemoryInfoData::read(yarp::os::idl::WireReader& reader)
{
    if (!read_totalSpace(reader)) {
        return false;
    }
    if (!read_freeSpace(reader)) {
        return false;
    }
    if (reader.isError()) {
        return false;
    }
    return true;
}

// Read structure on a Connection
bool MemoryInfoData::read(yarp::os::ConnectionReader& connection)
{
    yarp::os::idl::WireReader reader(connection);
    if (!reader.readListHeader(2)) {
        return false;
    }
    if (!read(reader)) {
        return false;
    }
    return true;
}

// Write structure on a Wire
bool MemoryInfoData::write(const yarp::os::idl::WireWriter& writer) const
{
    if (!write_totalSpace(writer)) {
        return false;
    }
    if (!write_freeSpace(writer)) {
        return false;
    }
    if (writer.isError()) {
        return false;
    }
    return true;
}

// Write structure on a Connection
bool MemoryInfoData::write(yarp::os::ConnectionWriter& connection) const
{
    yarp::os::idl::WireWriter writer(connection);
    if (!writer.writeListHeader(2)) {
        return false;
    }
    if (!write(writer)) {
        return false;
    }
    return true;
}

// Convert to a printable string
std::string MemoryInfoData::toString() const
{
    yarp::os::Bottle b;
    if (!yarp::os::Portable::copyPortable(*this, b)) {
        return {};
    }
    return b.toString();
}

// read totalSpace field
bool MemoryInfoData::read_totalSpace(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readI32(totalSpace)) {
        reader.fail();
        return false;
    }
    return true;
}

// write totalSpace field
bool MemoryInfoData::write_totalSpace(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeI32(totalSpace)) {
        return false;
    }
    return true;
}

// read (nested) totalSpace field
bool MemoryInfoData::nested_read_totalSpace(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readI32(totalSpace)) {
        reader.fail();
        return false;
    }
    return true;
}

// write (nested) totalSpace field
bool MemoryInfoData::nested_write_totalSpace(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeI32(totalSpace)) {
        return false;
    }
    return true;
}

// read freeSpace field
bool MemoryInfoData::read_freeSpace(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readI32(freeSpace)) {
        reader.fail();
        return false;
    }
    return true;
}

// write freeSpace field
bool MemoryInfoData::write_freeSpace(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeI32(freeSpace)) {
        return false;
    }
    return true;
}

// read (nested) freeSpace field
bool MemoryInfoData::nested_read_freeSpace(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readI32(freeSpace)) {
        reader.fail();
        return false;
    }
    return true;
}

// write (nested) freeSpace field
bool MemoryInfoData::nested_write_freeSpace(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeI32(freeSpace)) {
        return false;
    }
    return true;
}

} // namespace yarp::os
