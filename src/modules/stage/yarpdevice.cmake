
SET(YARPDEV_NAMES "stage")
SET(YARPDEV_WRAPPERS "controlboard")
SET(YARPDEV_INCLUDES "StageControl.h")
SET(YARPDEV_TYPES "StageControl")

IF (ENABLE_stage)
	MESSAGE(STATUS "checking for STAGE libraries etc")
	FIND_PACKAGE(Stage REQUIRED)
	IF (STAGE_FOUND)
		INCLUDE_DIRECTORIES(${STATE_INCLUDE_DIR})
		LINK_LIBRARIES(${STAGE_LIBRARY})
	ELSE (STAGE_FOUND)
		MESSAGE(STATUS "STAGE libraries not found")
	ENDIF(STAGE_FOUND)
ENDIF (ENABLE_stage)
