
PROJECT(test)

SET(YARP_ADMIN $ENV{YARP_ADMIN})

IF (NOT YARP_ADMIN)
   MESSAGE(ERROR "this device is not ready to build yet")
ENDIF (NOT YARP_ADMIN)

FIND_PACKAGE(YARP REQUIRED)

INCLUDE_DIRECTORIES(.)

LINK_LIBRARIES(vfw32)
ADD_EXECUTABLE(vfw_test VfwGrabber.cpp)
