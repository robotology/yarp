# LIST the available devices for a specific platform
# One day this list could be populated automatically.

IF (WIN32 AND NOT CYGWIN)
   SET(AVAILABLE_DEVICES dragonfly microphone jrkerr dimax_u2c)
   SET(AVAILABLE_DEVICES ${AVAILABLE_DEVICES} fob cyberglove e504 joypres)
ENDIF (WIN32 AND NOT CYGWIN)

IF (UNIX)
   SET(AVAILABLE_DEVICES firewirecamera dragonfly microphone urbtc)
ENDIF (UNIX)

SET(AVAILABLE_DEVICES ${AVAILABLE_DEVICES} opencv_grabber ffmpeg portaudio serial)