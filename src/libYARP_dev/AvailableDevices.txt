# LIST the available devices for a specific platform
# One day this list could be populated automatically.

IF (WIN32 AND NOT CYGWIN)
   SET(AVAILABLE_DEVICES dragonfly microphone inertiacube2 XSensMTx james jrkerr dimax_u2c)
   SET(AVAILABLE_DEVICES ${AVAILABLE_DEVICES} picolo_grabber fob cyberglove e504 joypres mei)
ENDIF (WIN32 AND NOT CYGWIN)

IF (UNIX)
   SET(AVAILABLE_DEVICES dragonfly microphone XSensMTx inertiacube2 james urbtc)
ENDIF (UNIX)

SET(AVAILABLE_DEVICES ${AVAILABLE_DEVICES} opencv_grabber ffmpeg esdMotionControl portaudio)