
This is an experimental version of the yarp name server that uses a
real database (sqlite) to maintain the state of the yarp network.

It has the functionality of the standard name server, but is not 
completely tested.

====================================================================
COMPILATION
===========

To compile:
  
  set the "CREATE_YARPSERVER3" option in CMake while compiling YARP.

