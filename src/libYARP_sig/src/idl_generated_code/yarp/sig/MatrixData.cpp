/*
 * SPDX-FileCopyrightText: 2006-2021 Istituto Italiano di Tecnologia (IIT)
 * SPDX-License-Identifier: BSD-3-Clause
 */

// Autogenerated by Thrift Compiler (0.14.1-yarped)
//
// This is an automatically generated file.
// It could get re-generated if the ALLOW_IDL_GENERATION flag is on.

#include <yarp/sig/MatrixData.h>

namespace yarp::sig {

// Constructor with field values
MatrixData::MatrixData(const std::int32_t nrows,
                       const std::int32_t ncols,
                       const std::vector<double>& storage) :
        WirePortable(),
        nrows(nrows),
        ncols(ncols),
        storage(storage)
{
}

// Read structure on a Wire
bool MatrixData::read(yarp::os::idl::WireReader& reader)
{
    if (!read_nrows(reader)) {
        return false;
    }
    if (!read_ncols(reader)) {
        return false;
    }
    if (!read_storage(reader)) {
        return false;
    }
    if (reader.isError()) {
        return false;
    }
    return true;
}

// Read structure on a Connection
bool MatrixData::read(yarp::os::ConnectionReader& connection)
{
    yarp::os::idl::WireReader reader(connection);
    if (!reader.readListHeader(3)) {
        return false;
    }
    if (!read(reader)) {
        return false;
    }
    return true;
}

// Write structure on a Wire
bool MatrixData::write(const yarp::os::idl::WireWriter& writer) const
{
    if (!write_nrows(writer)) {
        return false;
    }
    if (!write_ncols(writer)) {
        return false;
    }
    if (!write_storage(writer)) {
        return false;
    }
    if (writer.isError()) {
        return false;
    }
    return true;
}

// Write structure on a Connection
bool MatrixData::write(yarp::os::ConnectionWriter& connection) const
{
    yarp::os::idl::WireWriter writer(connection);
    if (!writer.writeListHeader(3)) {
        return false;
    }
    if (!write(writer)) {
        return false;
    }
    return true;
}

// Convert to a printable string
std::string MatrixData::toString() const
{
    yarp::os::Bottle b;
    if (!yarp::os::Portable::copyPortable(*this, b)) {
        return {};
    }
    return b.toString();
}

// read nrows field
bool MatrixData::read_nrows(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readI32(nrows)) {
        reader.fail();
        return false;
    }
    return true;
}

// write nrows field
bool MatrixData::write_nrows(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeI32(nrows)) {
        return false;
    }
    return true;
}

// read (nested) nrows field
bool MatrixData::nested_read_nrows(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readI32(nrows)) {
        reader.fail();
        return false;
    }
    return true;
}

// write (nested) nrows field
bool MatrixData::nested_write_nrows(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeI32(nrows)) {
        return false;
    }
    return true;
}

// read ncols field
bool MatrixData::read_ncols(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readI32(ncols)) {
        reader.fail();
        return false;
    }
    return true;
}

// write ncols field
bool MatrixData::write_ncols(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeI32(ncols)) {
        return false;
    }
    return true;
}

// read (nested) ncols field
bool MatrixData::nested_read_ncols(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readI32(ncols)) {
        reader.fail();
        return false;
    }
    return true;
}

// write (nested) ncols field
bool MatrixData::nested_write_ncols(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeI32(ncols)) {
        return false;
    }
    return true;
}

// read storage field
bool MatrixData::read_storage(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    {
        size_t _csize;
        yarp::os::idl::WireState _etype;
        reader.readListBegin(_etype, _csize);
        // WireReader removes BOTTLE_TAG_LIST from the tag
        constexpr int expected_tag = ((BOTTLE_TAG_FLOAT64) & (~BOTTLE_TAG_LIST));
        if constexpr (expected_tag != 0) {
            if (_csize != 0 && _etype.code != expected_tag) {
                return false;
            }
        }
        storage.resize(_csize);
        if (_csize != 0 && !reader.readBlock(reinterpret_cast<char*>(storage.data()), storage.size() * sizeof(double))) {
            return false;
        }
        reader.readListEnd();
    }
    return true;
}

// write storage field
bool MatrixData::write_storage(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeListBegin(BOTTLE_TAG_FLOAT64, storage.size())) {
        return false;
    }
    if (!writer.writeBlock(reinterpret_cast<const char*>(storage.data()), storage.size() * sizeof(double))) {
        return false;
    }
    if (!writer.writeListEnd()) {
        return false;
    }
    return true;
}

// read (nested) storage field
bool MatrixData::nested_read_storage(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    {
        size_t _csize;
        yarp::os::idl::WireState _etype;
        reader.readListBegin(_etype, _csize);
        // WireReader removes BOTTLE_TAG_LIST from the tag
        constexpr int expected_tag = ((BOTTLE_TAG_FLOAT64) & (~BOTTLE_TAG_LIST));
        if constexpr (expected_tag != 0) {
            if (_csize != 0 && _etype.code != expected_tag) {
                return false;
            }
        }
        storage.resize(_csize);
        if (_csize != 0 && !reader.readBlock(reinterpret_cast<char*>(storage.data()), storage.size() * sizeof(double))) {
            return false;
        }
        reader.readListEnd();
    }
    return true;
}

// write (nested) storage field
bool MatrixData::nested_write_storage(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeListBegin(BOTTLE_TAG_FLOAT64, storage.size())) {
        return false;
    }
    if (!writer.writeBlock(reinterpret_cast<const char*>(storage.data()), storage.size() * sizeof(double))) {
        return false;
    }
    if (!writer.writeListEnd()) {
        return false;
    }
    return true;
}

} // namespace yarp::sig
