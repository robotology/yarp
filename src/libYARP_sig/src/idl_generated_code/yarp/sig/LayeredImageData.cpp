/*
 * SPDX-FileCopyrightText: 2006-2021 Istituto Italiano di Tecnologia (IIT)
 * SPDX-License-Identifier: BSD-3-Clause
 */

// Autogenerated by Thrift Compiler (0.14.1-yarped)
//
// This is an automatically generated file.
// It could get re-generated if the ALLOW_IDL_GENERATION flag is on.

#include <yarp/sig/LayeredImageData.h>

namespace yarp::sig {

// Constructor with field values
LayeredImageData::LayeredImageData(const yarp::sig::FlexImage& background,
                                   const std::vector<yarp::sig::ImageLayer>& layers) :
        WirePortable(),
        background(background),
        layers(layers)
{
}

// Read structure on a Wire
bool LayeredImageData::read(yarp::os::idl::WireReader& reader)
{
    if (!nested_read_background(reader)) {
        return false;
    }
    if (!read_layers(reader)) {
        return false;
    }
    if (reader.isError()) {
        return false;
    }
    return true;
}

// Read structure on a Connection
bool LayeredImageData::read(yarp::os::ConnectionReader& connection)
{
    yarp::os::idl::WireReader reader(connection);
    if (!reader.readListHeader(2)) {
        return false;
    }
    if (!read(reader)) {
        return false;
    }
    return true;
}

// Write structure on a Wire
bool LayeredImageData::write(const yarp::os::idl::WireWriter& writer) const
{
    if (!nested_write_background(writer)) {
        return false;
    }
    if (!write_layers(writer)) {
        return false;
    }
    if (writer.isError()) {
        return false;
    }
    return true;
}

// Write structure on a Connection
bool LayeredImageData::write(yarp::os::ConnectionWriter& connection) const
{
    yarp::os::idl::WireWriter writer(connection);
    if (!writer.writeListHeader(2)) {
        return false;
    }
    if (!write(writer)) {
        return false;
    }
    return true;
}

// Convert to a printable string
std::string LayeredImageData::toString() const
{
    yarp::os::Bottle b;
    if (!yarp::os::Portable::copyPortable(*this, b)) {
        return {};
    }
    return b.toString();
}

// read background field
bool LayeredImageData::read_background(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.read(background)) {
        reader.fail();
        return false;
    }
    return true;
}

// write background field
bool LayeredImageData::write_background(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.write(background)) {
        return false;
    }
    return true;
}

// read (nested) background field
bool LayeredImageData::nested_read_background(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readNested(background)) {
        reader.fail();
        return false;
    }
    return true;
}

// write (nested) background field
bool LayeredImageData::nested_write_background(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeNested(background)) {
        return false;
    }
    return true;
}

// read layers field
bool LayeredImageData::read_layers(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    {
        size_t _csize;
        yarp::os::idl::WireState _etype;
        reader.readListBegin(_etype, _csize);
        // WireReader removes BOTTLE_TAG_LIST from the tag
        constexpr int expected_tag = ((BOTTLE_TAG_LIST) & (~BOTTLE_TAG_LIST));
        if constexpr (expected_tag != 0) {
            if (_csize != 0 && _etype.code != expected_tag) {
                return false;
            }
        }
        layers.resize(_csize);
        for (size_t _i0 = 0; _i0 < _csize; ++_i0) {
            if (reader.noMore()) {
                reader.fail();
                return false;
            }
            if (!reader.readNested(layers[_i0])) {
                reader.fail();
                return false;
            }
        }
        reader.readListEnd();
    }
    return true;
}

// write layers field
bool LayeredImageData::write_layers(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeListBegin(BOTTLE_TAG_LIST, layers.size())) {
        return false;
    }
    for (const auto& _item : layers) {
        if (!writer.writeNested(_item)) {
            return false;
        }
    }
    if (!writer.writeListEnd()) {
        return false;
    }
    return true;
}

// read (nested) layers field
bool LayeredImageData::nested_read_layers(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    {
        size_t _csize;
        yarp::os::idl::WireState _etype;
        reader.readListBegin(_etype, _csize);
        // WireReader removes BOTTLE_TAG_LIST from the tag
        constexpr int expected_tag = ((BOTTLE_TAG_LIST) & (~BOTTLE_TAG_LIST));
        if constexpr (expected_tag != 0) {
            if (_csize != 0 && _etype.code != expected_tag) {
                return false;
            }
        }
        layers.resize(_csize);
        for (size_t _i0 = 0; _i0 < _csize; ++_i0) {
            if (reader.noMore()) {
                reader.fail();
                return false;
            }
            if (!reader.readNested(layers[_i0])) {
                reader.fail();
                return false;
            }
        }
        reader.readListEnd();
    }
    return true;
}

// write (nested) layers field
bool LayeredImageData::nested_write_layers(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeListBegin(BOTTLE_TAG_LIST, layers.size())) {
        return false;
    }
    for (const auto& _item : layers) {
        if (!writer.writeNested(_item)) {
            return false;
        }
    }
    if (!writer.writeListEnd()) {
        return false;
    }
    return true;
}

} // namespace yarp::sig
