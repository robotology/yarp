/*
 * SPDX-FileCopyrightText: 2006-2021 Istituto Italiano di Tecnologia (IIT)
 * SPDX-License-Identifier: BSD-3-Clause
 */

// Autogenerated by Thrift Compiler (0.14.1-yarped)
//
// This is an automatically generated file.
// It could get re-generated if the ALLOW_IDL_GENERATION flag is on.

#include <yarp/sig/ImageLayerData.h>

namespace yarp::sig {

// Constructor with field values
ImageLayerData::ImageLayerData(const bool enable,
                               const yarp::sig::FlexImage& layer,
                               const yarp::sig::ImageColorKey& colorkey,
                               const yarp::sig::ImageAlphaData& alpha,
                               const bool can_be_compressed,
                               const std::int32_t offset_x,
                               const std::int32_t offset_y) :
        WirePortable(),
        enable(enable),
        layer(layer),
        colorkey(colorkey),
        alpha(alpha),
        can_be_compressed(can_be_compressed),
        offset_x(offset_x),
        offset_y(offset_y)
{
}

// Read structure on a Wire
bool ImageLayerData::read(yarp::os::idl::WireReader& reader)
{
    if (!read_enable(reader)) {
        return false;
    }
    if (!nested_read_layer(reader)) {
        return false;
    }
    if (!nested_read_colorkey(reader)) {
        return false;
    }
    if (!nested_read_alpha(reader)) {
        return false;
    }
    if (!read_can_be_compressed(reader)) {
        return false;
    }
    if (!read_offset_x(reader)) {
        return false;
    }
    if (!read_offset_y(reader)) {
        return false;
    }
    if (reader.isError()) {
        return false;
    }
    return true;
}

// Read structure on a Connection
bool ImageLayerData::read(yarp::os::ConnectionReader& connection)
{
    yarp::os::idl::WireReader reader(connection);
    if (!reader.readListHeader(7)) {
        return false;
    }
    if (!read(reader)) {
        return false;
    }
    return true;
}

// Write structure on a Wire
bool ImageLayerData::write(const yarp::os::idl::WireWriter& writer) const
{
    if (!write_enable(writer)) {
        return false;
    }
    if (!nested_write_layer(writer)) {
        return false;
    }
    if (!nested_write_colorkey(writer)) {
        return false;
    }
    if (!nested_write_alpha(writer)) {
        return false;
    }
    if (!write_can_be_compressed(writer)) {
        return false;
    }
    if (!write_offset_x(writer)) {
        return false;
    }
    if (!write_offset_y(writer)) {
        return false;
    }
    if (writer.isError()) {
        return false;
    }
    return true;
}

// Write structure on a Connection
bool ImageLayerData::write(yarp::os::ConnectionWriter& connection) const
{
    yarp::os::idl::WireWriter writer(connection);
    if (!writer.writeListHeader(7)) {
        return false;
    }
    if (!write(writer)) {
        return false;
    }
    return true;
}

// Convert to a printable string
std::string ImageLayerData::toString() const
{
    yarp::os::Bottle b;
    if (!yarp::os::Portable::copyPortable(*this, b)) {
        return {};
    }
    return b.toString();
}

// read enable field
bool ImageLayerData::read_enable(yarp::os::idl::WireReader& reader)
{
    if (!reader.readBool(enable)) {
        enable = true;
    }
    return true;
}

// write enable field
bool ImageLayerData::write_enable(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeBool(enable)) {
        return false;
    }
    return true;
}

// read (nested) enable field
bool ImageLayerData::nested_read_enable(yarp::os::idl::WireReader& reader)
{
    if (!reader.readBool(enable)) {
        enable = true;
    }
    return true;
}

// write (nested) enable field
bool ImageLayerData::nested_write_enable(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeBool(enable)) {
        return false;
    }
    return true;
}

// read layer field
bool ImageLayerData::read_layer(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.read(layer)) {
        reader.fail();
        return false;
    }
    return true;
}

// write layer field
bool ImageLayerData::write_layer(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.write(layer)) {
        return false;
    }
    return true;
}

// read (nested) layer field
bool ImageLayerData::nested_read_layer(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readNested(layer)) {
        reader.fail();
        return false;
    }
    return true;
}

// write (nested) layer field
bool ImageLayerData::nested_write_layer(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeNested(layer)) {
        return false;
    }
    return true;
}

// read colorkey field
bool ImageLayerData::read_colorkey(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.read(colorkey)) {
        reader.fail();
        return false;
    }
    return true;
}

// write colorkey field
bool ImageLayerData::write_colorkey(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.write(colorkey)) {
        return false;
    }
    return true;
}

// read (nested) colorkey field
bool ImageLayerData::nested_read_colorkey(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readNested(colorkey)) {
        reader.fail();
        return false;
    }
    return true;
}

// write (nested) colorkey field
bool ImageLayerData::nested_write_colorkey(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeNested(colorkey)) {
        return false;
    }
    return true;
}

// read alpha field
bool ImageLayerData::read_alpha(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.read(alpha)) {
        reader.fail();
        return false;
    }
    return true;
}

// write alpha field
bool ImageLayerData::write_alpha(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.write(alpha)) {
        return false;
    }
    return true;
}

// read (nested) alpha field
bool ImageLayerData::nested_read_alpha(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readNested(alpha)) {
        reader.fail();
        return false;
    }
    return true;
}

// write (nested) alpha field
bool ImageLayerData::nested_write_alpha(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeNested(alpha)) {
        return false;
    }
    return true;
}

// read can_be_compressed field
bool ImageLayerData::read_can_be_compressed(yarp::os::idl::WireReader& reader)
{
    if (!reader.readBool(can_be_compressed)) {
        can_be_compressed = true;
    }
    return true;
}

// write can_be_compressed field
bool ImageLayerData::write_can_be_compressed(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeBool(can_be_compressed)) {
        return false;
    }
    return true;
}

// read (nested) can_be_compressed field
bool ImageLayerData::nested_read_can_be_compressed(yarp::os::idl::WireReader& reader)
{
    if (!reader.readBool(can_be_compressed)) {
        can_be_compressed = true;
    }
    return true;
}

// write (nested) can_be_compressed field
bool ImageLayerData::nested_write_can_be_compressed(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeBool(can_be_compressed)) {
        return false;
    }
    return true;
}

// read offset_x field
bool ImageLayerData::read_offset_x(yarp::os::idl::WireReader& reader)
{
    if (!reader.readI32(offset_x)) {
        offset_x = 0;
    }
    return true;
}

// write offset_x field
bool ImageLayerData::write_offset_x(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeI32(offset_x)) {
        return false;
    }
    return true;
}

// read (nested) offset_x field
bool ImageLayerData::nested_read_offset_x(yarp::os::idl::WireReader& reader)
{
    if (!reader.readI32(offset_x)) {
        offset_x = 0;
    }
    return true;
}

// write (nested) offset_x field
bool ImageLayerData::nested_write_offset_x(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeI32(offset_x)) {
        return false;
    }
    return true;
}

// read offset_y field
bool ImageLayerData::read_offset_y(yarp::os::idl::WireReader& reader)
{
    if (!reader.readI32(offset_y)) {
        offset_y = 0;
    }
    return true;
}

// write offset_y field
bool ImageLayerData::write_offset_y(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeI32(offset_y)) {
        return false;
    }
    return true;
}

// read (nested) offset_y field
bool ImageLayerData::nested_read_offset_y(yarp::os::idl::WireReader& reader)
{
    if (!reader.readI32(offset_y)) {
        offset_y = 0;
    }
    return true;
}

// write (nested) offset_y field
bool ImageLayerData::nested_write_offset_y(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeI32(offset_y)) {
        return false;
    }
    return true;
}

} // namespace yarp::sig
