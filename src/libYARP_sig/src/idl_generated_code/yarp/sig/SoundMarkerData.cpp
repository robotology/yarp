/*
 * SPDX-FileCopyrightText: 2006-2021 Istituto Italiano di Tecnologia (IIT)
 * SPDX-License-Identifier: BSD-3-Clause
 */

// Autogenerated by Thrift Compiler (0.14.1-yarped)
//
// This is an automatically generated file.
// It could get re-generated if the ALLOW_IDL_GENERATION flag is on.

#include <yarp/sig/SoundMarkerData.h>

namespace yarp::sig {

// Constructor with field values
SoundMarkerData::SoundMarkerData(const std::string& label,
                                 const std::int32_t channel,
                                 const std::int32_t sample_id) :
        WirePortable(),
        label(label),
        channel(channel),
        sample_id(sample_id)
{
}

// Read structure on a Wire
bool SoundMarkerData::read(yarp::os::idl::WireReader& reader)
{
    if (!read_label(reader)) {
        return false;
    }
    if (!read_channel(reader)) {
        return false;
    }
    if (!read_sample_id(reader)) {
        return false;
    }
    if (reader.isError()) {
        return false;
    }
    return true;
}

// Read structure on a Connection
bool SoundMarkerData::read(yarp::os::ConnectionReader& connection)
{
    yarp::os::idl::WireReader reader(connection);
    if (!reader.readListHeader(3)) {
        return false;
    }
    if (!read(reader)) {
        return false;
    }
    return true;
}

// Write structure on a Wire
bool SoundMarkerData::write(const yarp::os::idl::WireWriter& writer) const
{
    if (!write_label(writer)) {
        return false;
    }
    if (!write_channel(writer)) {
        return false;
    }
    if (!write_sample_id(writer)) {
        return false;
    }
    if (writer.isError()) {
        return false;
    }
    return true;
}

// Write structure on a Connection
bool SoundMarkerData::write(yarp::os::ConnectionWriter& connection) const
{
    yarp::os::idl::WireWriter writer(connection);
    if (!writer.writeListHeader(3)) {
        return false;
    }
    if (!write(writer)) {
        return false;
    }
    return true;
}

// Convert to a printable string
std::string SoundMarkerData::toString() const
{
    yarp::os::Bottle b;
    if (!yarp::os::Portable::copyPortable(*this, b)) {
        return {};
    }
    return b.toString();
}

// read label field
bool SoundMarkerData::read_label(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readString(label)) {
        reader.fail();
        return false;
    }
    return true;
}

// write label field
bool SoundMarkerData::write_label(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeString(label)) {
        return false;
    }
    return true;
}

// read (nested) label field
bool SoundMarkerData::nested_read_label(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readString(label)) {
        reader.fail();
        return false;
    }
    return true;
}

// write (nested) label field
bool SoundMarkerData::nested_write_label(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeString(label)) {
        return false;
    }
    return true;
}

// read channel field
bool SoundMarkerData::read_channel(yarp::os::idl::WireReader& reader)
{
    if (!reader.readI32(channel)) {
        channel = -1;
    }
    return true;
}

// write channel field
bool SoundMarkerData::write_channel(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeI32(channel)) {
        return false;
    }
    return true;
}

// read (nested) channel field
bool SoundMarkerData::nested_read_channel(yarp::os::idl::WireReader& reader)
{
    if (!reader.readI32(channel)) {
        channel = -1;
    }
    return true;
}

// write (nested) channel field
bool SoundMarkerData::nested_write_channel(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeI32(channel)) {
        return false;
    }
    return true;
}

// read sample_id field
bool SoundMarkerData::read_sample_id(yarp::os::idl::WireReader& reader)
{
    if (!reader.readI32(sample_id)) {
        sample_id = 0;
    }
    return true;
}

// write sample_id field
bool SoundMarkerData::write_sample_id(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeI32(sample_id)) {
        return false;
    }
    return true;
}

// read (nested) sample_id field
bool SoundMarkerData::nested_read_sample_id(yarp::os::idl::WireReader& reader)
{
    if (!reader.readI32(sample_id)) {
        sample_id = 0;
    }
    return true;
}

// write (nested) sample_id field
bool SoundMarkerData::nested_write_sample_id(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeI32(sample_id)) {
        return false;
    }
    return true;
}

} // namespace yarp::sig
