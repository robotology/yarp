IF (COMPILE_PLUGIN_LIBRARY)
  PREPARE_CARRIER(human_carrier TYPE HumanCarrier INCLUDE HumanCarrier.h)
ENDIF (COMPILE_PLUGIN_LIBRARY)

IF (NOT SKIP_human_carrier)
  FIND_PACKAGE(YARP REQUIRED)
  INCLUDE_DIRECTORIES(${YARP_INCLUDE_DIRS})
  INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})
  ADD_LIBRARY(human_carrier HumanCarrier.h HumanStream.h HumanCarrier.cpp HumanStream.cpp)
  TARGET_LINK_LIBRARIES(human_carrier ${YARP_LIBRARIES})
ENDIF (NOT SKIP_human_carrier)
