IF (COEXIST_WITH_YARP1 AND YARP1_FOUND)
   INCLUDE_DIRECTORIES(${YARP1_INCLUDE_DIR})
   # YARP1 config files usually placed in a sub-directory given by OS_TAG
   INCLUDE_DIRECTORIES(${YARP1_INCLUDE_DIR}/${OS_TAG})
ENDIF (COEXIST_WITH_YARP1 AND YARP1_FOUND)


SET(NEED_LIBS YARP_OS)
SET(ALT_INCLUDES ${YARP1_INCLUDE_DIR} ${YARP1_INCLUDE_DIR}/${OS_TAG})
YarpLibrary(YARP_OS_compat os_compat)
