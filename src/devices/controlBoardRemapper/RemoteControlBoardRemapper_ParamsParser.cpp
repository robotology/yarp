/*
 * SPDX-FileCopyrightText: 2023-2023 Istituto Italiano di Tecnologia (IIT)
 * SPDX-License-Identifier: LGPL-2.1-or-later
 */


// Generated by yarpDeviceParamParserGenerator (2.2)
// This is an automatically generated file. Please do not edit it.
// It will be re-generated if the cmake flag ALLOW_DEVICE_PARAM_PARSER_GENERATION is ON.

// Generated on: Tue Aug 18 16:11:41 2026


#include "RemoteControlBoardRemapper_ParamsParser.h"
#include <yarp/os/LogStream.h>
#include <yarp/os/Value.h>

namespace {
    YARP_LOG_COMPONENT(RemoteControlBoardRemapperParamsCOMPONENT, "yarp.device.RemoteControlBoardRemapper")
}


RemoteControlBoardRemapper_ParamsParser::RemoteControlBoardRemapper_ParamsParser()
{
}


std::vector<std::string> RemoteControlBoardRemapper_ParamsParser::getListOfParams() const
{
    std::vector<std::string> params;
    params.push_back("remoteControlBoards");
    params.push_back("axesNames");
    params.push_back("localPortPrefix");
    params.push_back("verbose");
    return params;
}


bool RemoteControlBoardRemapper_ParamsParser::getParamValue(const std::string& paramName, std::string& paramValue) const
{
    if (paramName =="remoteControlBoards")
    {
        std::ostringstream oss;
        oss << "( ";
        for (auto x : m_remoteControlBoards)
        {
            oss << x << " ";
        }
        oss << " )";
        paramValue = oss.str();
        return true;
    }
    if (paramName =="axesNames")
    {
        std::ostringstream oss;
        oss << "( ";
        for (auto x : m_axesNames)
        {
            oss << x << " ";
        }
        oss << " )";
        paramValue = oss.str();
        return true;
    }
    if (paramName =="localPortPrefix")
    {
        paramValue = m_localPortPrefix;
        return true;
    }
    if (paramName =="verbose")
    {
        if (m_verbose==true) paramValue = "true";
        else paramValue = "false";
        return true;
    }

    yError() <<"parameter '" << paramName << "' was not found";
    return false;

}


std::string RemoteControlBoardRemapper_ParamsParser::getConfiguration() const
{
    //This is a sub-optimal solution.
    //Ideally getConfiguration() should return all parameters but it is currently
    //returning only user provided parameters (excluding default values)
    //This behaviour will be fixed in the near future.
    std::string s_cfg = m_provided_configuration;
    return s_cfg;
}

bool      RemoteControlBoardRemapper_ParamsParser::parseParams(const yarp::os::Searchable & config)
{
    //Check for --help option
    if (config.check("help"))
    {
        yCInfo(RemoteControlBoardRemapperParamsCOMPONENT) << getDocumentationOfDeviceParams();
    }

    m_provided_configuration = config.toString();
    yarp::os::Property prop_check(m_provided_configuration.c_str());
    //Parser of parameter remoteControlBoards
    {
        if (config.check("remoteControlBoards"))
        {
            {
                m_remoteControlBoards.clear();
                yarp::os::Bottle* tempBot = config.find("remoteControlBoards").asList();
                if (tempBot)
                {
                    std::string tempBots = tempBot->toString();
                    for (size_t i=0; i<tempBot->size(); i++)
                    {
                        m_remoteControlBoards.push_back(tempBot->get(i).asString());
                    }
                }
                else
                {
                     yCError(RemoteControlBoardRemapperParamsCOMPONENT) <<"parameter 'remoteControlBoards' is not a properly formatted bottle";
                }
            }
            yCInfo(RemoteControlBoardRemapperParamsCOMPONENT) << "Parameter 'remoteControlBoards' using value:" << m_remoteControlBoards;
        }
        else
        {
            yCError(RemoteControlBoardRemapperParamsCOMPONENT) << "Mandatory parameter 'remoteControlBoards' not found!";
            yCError(RemoteControlBoardRemapperParamsCOMPONENT) << "Description of the parameter: Ordered list of the axes that are part of the remapped device.";
            return false;
        }
        prop_check.unput("remoteControlBoards");
    }

    //Parser of parameter axesNames
    {
        if (config.check("axesNames"))
        {
            {
                m_axesNames.clear();
                yarp::os::Bottle* tempBot = config.find("axesNames").asList();
                if (tempBot)
                {
                    std::string tempBots = tempBot->toString();
                    for (size_t i=0; i<tempBot->size(); i++)
                    {
                        m_axesNames.push_back(tempBot->get(i).asString());
                    }
                }
                else
                {
                     yCError(RemoteControlBoardRemapperParamsCOMPONENT) <<"parameter 'axesNames' is not a properly formatted bottle";
                }
            }
            yCInfo(RemoteControlBoardRemapperParamsCOMPONENT) << "Parameter 'axesNames' using value:" << m_axesNames;
        }
        else
        {
            yCError(RemoteControlBoardRemapperParamsCOMPONENT) << "Mandatory parameter 'axesNames' not found!";
            yCError(RemoteControlBoardRemapperParamsCOMPONENT) << "Description of the parameter: List of remote prefix used by the remote controlboards";
            return false;
        }
        prop_check.unput("axesNames");
    }

    //Parser of parameter localPortPrefix
    {
        if (config.check("localPortPrefix"))
        {
            m_localPortPrefix = config.find("localPortPrefix").asString();
            yCInfo(RemoteControlBoardRemapperParamsCOMPONENT) << "Parameter 'localPortPrefix' using value:" << m_localPortPrefix;
        }
        else
        {
            yCError(RemoteControlBoardRemapperParamsCOMPONENT) << "Mandatory parameter 'localPortPrefix' not found!";
            yCError(RemoteControlBoardRemapperParamsCOMPONENT) << "Description of the parameter: All ports opened by this device will start with this prefix";
            return false;
        }
        prop_check.unput("localPortPrefix");
    }

    //Parser of parameter verbose
    {
        if (config.check("verbose"))
        {
            m_verbose = config.find("verbose").asBool();
            yCInfo(RemoteControlBoardRemapperParamsCOMPONENT) << "Parameter 'verbose' using value:" << m_verbose;
        }
        else
        {
            yCInfo(RemoteControlBoardRemapperParamsCOMPONENT) << "Parameter 'verbose' using DEFAULT value:" << m_verbose;
        }
        prop_check.unput("verbose");
    }

    /*
    //This code check if the user set some parameter which are not check by the parser
    //If the parser is set in strict mode, this will generate an error
    if (prop_check.size() > 0)
    {
        bool extra_params_found = false;
        for (auto it=prop_check.begin(); it!=prop_check.end(); it++)
        {
            if (m_parser_is_strict)
            {
                yCError(RemoteControlBoardRemapperParamsCOMPONENT) << "User asking for parameter: "<<it->name <<" which is unknown to this parser!";
                extra_params_found = true;
            }
            else
            {
                yCWarning(RemoteControlBoardRemapperParamsCOMPONENT) << "User asking for parameter: "<< it->name <<" which is unknown to this parser!";
            }
        }

       if (m_parser_is_strict && extra_params_found)
       {
           return false;
       }
    }
    */
    return true;
}


std::string      RemoteControlBoardRemapper_ParamsParser::getDocumentationOfDeviceParams() const
{
    std::string doc;
    doc = doc + std::string("\n=============================================\n");
    doc = doc + std::string("This is the help for device: RemoteControlBoardRemapper\n");
    doc = doc + std::string("\n");
    doc = doc + std::string("This is the list of the parameters accepted by the device:\n");
    doc = doc + std::string("'remoteControlBoards': Ordered list of the axes that are part of the remapped device.\n");
    doc = doc + std::string("'axesNames': List of remote prefix used by the remote controlboards\n");
    doc = doc + std::string("'localPortPrefix': All ports opened by this device will start with this prefix\n");
    doc = doc + std::string("'verbose': print additional output if enabled\n");
    doc = doc + std::string("\n");
    doc = doc + std::string("Here are some examples of invocation command with yarpdev, with all params:\n");
    doc = doc + " yarpdev --device remotecontrolboardremapper --remoteControlBoards <mandatory_value> --axesNames <mandatory_value> --localPortPrefix <mandatory_value> --verbose false\n";
    doc = doc + std::string("Using only mandatory params:\n");
    doc = doc + " yarpdev --device remotecontrolboardremapper --remoteControlBoards <mandatory_value> --axesNames <mandatory_value> --localPortPrefix <mandatory_value>\n";
    doc = doc + std::string("=============================================\n\n");    return doc;
}
