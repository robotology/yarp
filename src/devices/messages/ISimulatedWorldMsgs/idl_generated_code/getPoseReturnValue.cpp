/*
 * SPDX-FileCopyrightText: 2006-2021 Istituto Italiano di Tecnologia (IIT)
 * SPDX-License-Identifier: BSD-3-Clause
 */

// Autogenerated by Thrift Compiler (0.14.1-yarped)
//
// This is an automatically generated file.
// It could get re-generated if the ALLOW_IDL_GENERATION flag is on.

#include <getPoseReturnValue.h>

// Constructor with field values
getPoseReturnValue::getPoseReturnValue(const yarp::dev::ReturnValue& retVal,
                                       const yarp::sig::Pose6D& pose) :
        WirePortable(),
        retVal(retVal),
        pose(pose)
{
}

// Read structure on a Wire
bool getPoseReturnValue::read(yarp::os::idl::WireReader& reader)
{
    if (!nested_read_retVal(reader)) {
        return false;
    }
    if (!nested_read_pose(reader)) {
        return false;
    }
    if (reader.isError()) {
        return false;
    }
    return true;
}

// Read structure on a Connection
bool getPoseReturnValue::read(yarp::os::ConnectionReader& connection)
{
    yarp::os::idl::WireReader reader(connection);
    if (!reader.readListHeader(2)) {
        return false;
    }
    if (!read(reader)) {
        return false;
    }
    return true;
}

// Write structure on a Wire
bool getPoseReturnValue::write(const yarp::os::idl::WireWriter& writer) const
{
    if (!nested_write_retVal(writer)) {
        return false;
    }
    if (!nested_write_pose(writer)) {
        return false;
    }
    if (writer.isError()) {
        return false;
    }
    return true;
}

// Write structure on a Connection
bool getPoseReturnValue::write(yarp::os::ConnectionWriter& connection) const
{
    yarp::os::idl::WireWriter writer(connection);
    if (!writer.writeListHeader(2)) {
        return false;
    }
    if (!write(writer)) {
        return false;
    }
    return true;
}

// Convert to a printable string
std::string getPoseReturnValue::toString() const
{
    yarp::os::Bottle b;
    if (!yarp::os::Portable::copyPortable(*this, b)) {
        return {};
    }
    return b.toString();
}

// read retVal field
bool getPoseReturnValue::read_retVal(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.read(retVal)) {
        reader.fail();
        return false;
    }
    return true;
}

// write retVal field
bool getPoseReturnValue::write_retVal(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.write(retVal)) {
        return false;
    }
    return true;
}

// read (nested) retVal field
bool getPoseReturnValue::nested_read_retVal(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readNested(retVal)) {
        reader.fail();
        return false;
    }
    return true;
}

// write (nested) retVal field
bool getPoseReturnValue::nested_write_retVal(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeNested(retVal)) {
        return false;
    }
    return true;
}

// read pose field
bool getPoseReturnValue::read_pose(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.read(pose)) {
        reader.fail();
        return false;
    }
    return true;
}

// write pose field
bool getPoseReturnValue::write_pose(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.write(pose)) {
        return false;
    }
    return true;
}

// read (nested) pose field
bool getPoseReturnValue::nested_read_pose(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readNested(pose)) {
        reader.fail();
        return false;
    }
    return true;
}

// write (nested) pose field
bool getPoseReturnValue::nested_write_pose(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeNested(pose)) {
        return false;
    }
    return true;
}
