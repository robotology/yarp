/*
 * SPDX-FileCopyrightText: 2006-2021 Istituto Italiano di Tecnologia (IIT)
 * SPDX-License-Identifier: BSD-3-Clause
 */

// Autogenerated by Thrift Compiler (0.14.1-yarped)
//
// This is an automatically generated file.
// It could get re-generated if the ALLOW_IDL_GENERATION flag is on.

#include <getListReturnValue.h>

// Constructor with field values
getListReturnValue::getListReturnValue(const yarp::dev::ReturnValue& retVal,
                                       const std::vector<std::string>& listnames) :
        WirePortable(),
        retVal(retVal),
        listnames(listnames)
{
}

// Read structure on a Wire
bool getListReturnValue::read(yarp::os::idl::WireReader& reader)
{
    if (!nested_read_retVal(reader)) {
        return false;
    }
    if (!read_listnames(reader)) {
        return false;
    }
    if (reader.isError()) {
        return false;
    }
    return true;
}

// Read structure on a Connection
bool getListReturnValue::read(yarp::os::ConnectionReader& connection)
{
    yarp::os::idl::WireReader reader(connection);
    if (!reader.readListHeader(2)) {
        return false;
    }
    if (!read(reader)) {
        return false;
    }
    return true;
}

// Write structure on a Wire
bool getListReturnValue::write(const yarp::os::idl::WireWriter& writer) const
{
    if (!nested_write_retVal(writer)) {
        return false;
    }
    if (!write_listnames(writer)) {
        return false;
    }
    if (writer.isError()) {
        return false;
    }
    return true;
}

// Write structure on a Connection
bool getListReturnValue::write(yarp::os::ConnectionWriter& connection) const
{
    yarp::os::idl::WireWriter writer(connection);
    if (!writer.writeListHeader(2)) {
        return false;
    }
    if (!write(writer)) {
        return false;
    }
    return true;
}

// Convert to a printable string
std::string getListReturnValue::toString() const
{
    yarp::os::Bottle b;
    if (!yarp::os::Portable::copyPortable(*this, b)) {
        return {};
    }
    return b.toString();
}

// read retVal field
bool getListReturnValue::read_retVal(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.read(retVal)) {
        reader.fail();
        return false;
    }
    return true;
}

// write retVal field
bool getListReturnValue::write_retVal(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.write(retVal)) {
        return false;
    }
    return true;
}

// read (nested) retVal field
bool getListReturnValue::nested_read_retVal(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readNested(retVal)) {
        reader.fail();
        return false;
    }
    return true;
}

// write (nested) retVal field
bool getListReturnValue::nested_write_retVal(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeNested(retVal)) {
        return false;
    }
    return true;
}

// read listnames field
bool getListReturnValue::read_listnames(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    size_t _csize;
    yarp::os::idl::WireState _etype;
    reader.readListBegin(_etype, _csize);
    // WireReader removes BOTTLE_TAG_LIST from the tag
    constexpr int expected_tag = ((BOTTLE_TAG_STRING) & (~BOTTLE_TAG_LIST));
    if constexpr (expected_tag != 0) {
        if (_csize != 0 && _etype.code != expected_tag) {
            return false;
        }
    }
    listnames.resize(_csize);
    for (size_t _i = 0; _i < _csize; ++_i) {
        if (reader.noMore()) {
            reader.fail();
            return false;
        }
        if (!reader.readString(listnames[_i])) {
            reader.fail();
            return false;
        }
    }
    reader.readListEnd();
    return true;
}

// write listnames field
bool getListReturnValue::write_listnames(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeListBegin(BOTTLE_TAG_STRING, listnames.size())) {
        return false;
    }
    for (const auto& _item : listnames) {
        if (!writer.writeString(_item, true)) {
            return false;
        }
    }
    if (!writer.writeListEnd()) {
        return false;
    }
    return true;
}

// read (nested) listnames field
bool getListReturnValue::nested_read_listnames(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    size_t _csize;
    yarp::os::idl::WireState _etype;
    reader.readListBegin(_etype, _csize);
    // WireReader removes BOTTLE_TAG_LIST from the tag
    constexpr int expected_tag = ((BOTTLE_TAG_STRING) & (~BOTTLE_TAG_LIST));
    if constexpr (expected_tag != 0) {
        if (_csize != 0 && _etype.code != expected_tag) {
            return false;
        }
    }
    listnames.resize(_csize);
    for (size_t _i = 0; _i < _csize; ++_i) {
        if (reader.noMore()) {
            reader.fail();
            return false;
        }
        if (!reader.readString(listnames[_i])) {
            reader.fail();
            return false;
        }
    }
    reader.readListEnd();
    return true;
}

// write (nested) listnames field
bool getListReturnValue::nested_write_listnames(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeListBegin(BOTTLE_TAG_STRING, listnames.size())) {
        return false;
    }
    for (const auto& _item : listnames) {
        if (!writer.writeString(_item, true)) {
            return false;
        }
    }
    if (!writer.writeListEnd()) {
        return false;
    }
    return true;
}
