/*
 * SPDX-FileCopyrightText: 2006-2021 Istituto Italiano di Tecnologia (IIT)
 * SPDX-License-Identifier: BSD-3-Clause
 */

// Autogenerated by Thrift Compiler (0.14.1-yarped)
//
// This is an automatically generated file.
// It could get re-generated if the ALLOW_IDL_GENERATION flag is on.

#include <yarp/dev/AllJoyData.h>

namespace yarp::dev {

// Constructor with field values
AllJoyData::AllJoyData(const std::vector<double>& ButtonDataVal,
                       const std::vector<double>& AxisDataVal,
                       const std::vector<std::int8_t>& HatsDataVal,
                       const std::vector<MultipleTouches>& TouchDataVal,
                       const std::vector<StickData>& StickDataVal,
                       const std::vector<TrackballData>& TrackballDataVal) :
        WirePortable(),
        ButtonDataVal(ButtonDataVal),
        AxisDataVal(AxisDataVal),
        HatsDataVal(HatsDataVal),
        TouchDataVal(TouchDataVal),
        StickDataVal(StickDataVal),
        TrackballDataVal(TrackballDataVal)
{
}

// Read structure on a Wire
bool AllJoyData::read(yarp::os::idl::WireReader& reader)
{
    if (!read_ButtonDataVal(reader)) {
        return false;
    }
    if (!read_AxisDataVal(reader)) {
        return false;
    }
    if (!read_HatsDataVal(reader)) {
        return false;
    }
    if (!read_TouchDataVal(reader)) {
        return false;
    }
    if (!read_StickDataVal(reader)) {
        return false;
    }
    if (!read_TrackballDataVal(reader)) {
        return false;
    }
    if (reader.isError()) {
        return false;
    }
    return true;
}

// Read structure on a Connection
bool AllJoyData::read(yarp::os::ConnectionReader& connection)
{
    yarp::os::idl::WireReader reader(connection);
    if (!reader.readListHeader(6)) {
        return false;
    }
    if (!read(reader)) {
        return false;
    }
    return true;
}

// Write structure on a Wire
bool AllJoyData::write(const yarp::os::idl::WireWriter& writer) const
{
    if (!write_ButtonDataVal(writer)) {
        return false;
    }
    if (!write_AxisDataVal(writer)) {
        return false;
    }
    if (!write_HatsDataVal(writer)) {
        return false;
    }
    if (!write_TouchDataVal(writer)) {
        return false;
    }
    if (!write_StickDataVal(writer)) {
        return false;
    }
    if (!write_TrackballDataVal(writer)) {
        return false;
    }
    if (writer.isError()) {
        return false;
    }
    return true;
}

// Write structure on a Connection
bool AllJoyData::write(yarp::os::ConnectionWriter& connection) const
{
    yarp::os::idl::WireWriter writer(connection);
    if (!writer.writeListHeader(6)) {
        return false;
    }
    if (!write(writer)) {
        return false;
    }
    return true;
}

// Convert to a printable string
std::string AllJoyData::toString() const
{
    yarp::os::Bottle b;
    if (!yarp::os::Portable::copyPortable(*this, b)) {
        return {};
    }
    return b.toString();
}

// read ButtonDataVal field
bool AllJoyData::read_ButtonDataVal(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    {
        size_t _csize;
        yarp::os::idl::WireState _etype;
        reader.readListBegin(_etype, _csize);
        // WireReader removes BOTTLE_TAG_LIST from the tag
        constexpr int expected_tag = ((BOTTLE_TAG_FLOAT64) & (~BOTTLE_TAG_LIST));
        if constexpr (expected_tag != 0) {
            if (_csize != 0 && _etype.code != expected_tag) {
                return false;
            }
        }
        ButtonDataVal.resize(_csize);
        if (_csize != 0 && !reader.readBlock(reinterpret_cast<char*>(ButtonDataVal.data()), ButtonDataVal.size() * sizeof(double))) {
            return false;
        }
        reader.readListEnd();
    }
    return true;
}

// write ButtonDataVal field
bool AllJoyData::write_ButtonDataVal(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeListBegin(BOTTLE_TAG_FLOAT64, ButtonDataVal.size())) {
        return false;
    }
    if (!writer.writeBlock(reinterpret_cast<const char*>(ButtonDataVal.data()), ButtonDataVal.size() * sizeof(double))) {
        return false;
    }
    if (!writer.writeListEnd()) {
        return false;
    }
    return true;
}

// read (nested) ButtonDataVal field
bool AllJoyData::nested_read_ButtonDataVal(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    {
        size_t _csize;
        yarp::os::idl::WireState _etype;
        reader.readListBegin(_etype, _csize);
        // WireReader removes BOTTLE_TAG_LIST from the tag
        constexpr int expected_tag = ((BOTTLE_TAG_FLOAT64) & (~BOTTLE_TAG_LIST));
        if constexpr (expected_tag != 0) {
            if (_csize != 0 && _etype.code != expected_tag) {
                return false;
            }
        }
        ButtonDataVal.resize(_csize);
        if (_csize != 0 && !reader.readBlock(reinterpret_cast<char*>(ButtonDataVal.data()), ButtonDataVal.size() * sizeof(double))) {
            return false;
        }
        reader.readListEnd();
    }
    return true;
}

// write (nested) ButtonDataVal field
bool AllJoyData::nested_write_ButtonDataVal(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeListBegin(BOTTLE_TAG_FLOAT64, ButtonDataVal.size())) {
        return false;
    }
    if (!writer.writeBlock(reinterpret_cast<const char*>(ButtonDataVal.data()), ButtonDataVal.size() * sizeof(double))) {
        return false;
    }
    if (!writer.writeListEnd()) {
        return false;
    }
    return true;
}

// read AxisDataVal field
bool AllJoyData::read_AxisDataVal(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    {
        size_t _csize;
        yarp::os::idl::WireState _etype;
        reader.readListBegin(_etype, _csize);
        // WireReader removes BOTTLE_TAG_LIST from the tag
        constexpr int expected_tag = ((BOTTLE_TAG_FLOAT64) & (~BOTTLE_TAG_LIST));
        if constexpr (expected_tag != 0) {
            if (_csize != 0 && _etype.code != expected_tag) {
                return false;
            }
        }
        AxisDataVal.resize(_csize);
        if (_csize != 0 && !reader.readBlock(reinterpret_cast<char*>(AxisDataVal.data()), AxisDataVal.size() * sizeof(double))) {
            return false;
        }
        reader.readListEnd();
    }
    return true;
}

// write AxisDataVal field
bool AllJoyData::write_AxisDataVal(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeListBegin(BOTTLE_TAG_FLOAT64, AxisDataVal.size())) {
        return false;
    }
    if (!writer.writeBlock(reinterpret_cast<const char*>(AxisDataVal.data()), AxisDataVal.size() * sizeof(double))) {
        return false;
    }
    if (!writer.writeListEnd()) {
        return false;
    }
    return true;
}

// read (nested) AxisDataVal field
bool AllJoyData::nested_read_AxisDataVal(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    {
        size_t _csize;
        yarp::os::idl::WireState _etype;
        reader.readListBegin(_etype, _csize);
        // WireReader removes BOTTLE_TAG_LIST from the tag
        constexpr int expected_tag = ((BOTTLE_TAG_FLOAT64) & (~BOTTLE_TAG_LIST));
        if constexpr (expected_tag != 0) {
            if (_csize != 0 && _etype.code != expected_tag) {
                return false;
            }
        }
        AxisDataVal.resize(_csize);
        if (_csize != 0 && !reader.readBlock(reinterpret_cast<char*>(AxisDataVal.data()), AxisDataVal.size() * sizeof(double))) {
            return false;
        }
        reader.readListEnd();
    }
    return true;
}

// write (nested) AxisDataVal field
bool AllJoyData::nested_write_AxisDataVal(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeListBegin(BOTTLE_TAG_FLOAT64, AxisDataVal.size())) {
        return false;
    }
    if (!writer.writeBlock(reinterpret_cast<const char*>(AxisDataVal.data()), AxisDataVal.size() * sizeof(double))) {
        return false;
    }
    if (!writer.writeListEnd()) {
        return false;
    }
    return true;
}

// read HatsDataVal field
bool AllJoyData::read_HatsDataVal(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    {
        size_t _csize;
        yarp::os::idl::WireState _etype;
        reader.readListBegin(_etype, _csize);
        // WireReader removes BOTTLE_TAG_LIST from the tag
        constexpr int expected_tag = ((BOTTLE_TAG_INT8) & (~BOTTLE_TAG_LIST));
        if constexpr (expected_tag != 0) {
            if (_csize != 0 && _etype.code != expected_tag) {
                return false;
            }
        }
        HatsDataVal.resize(_csize);
        if (_csize != 0 && !reader.readBlock(reinterpret_cast<char*>(HatsDataVal.data()), HatsDataVal.size() * sizeof(std::int8_t))) {
            return false;
        }
        reader.readListEnd();
    }
    return true;
}

// write HatsDataVal field
bool AllJoyData::write_HatsDataVal(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeListBegin(BOTTLE_TAG_INT8, HatsDataVal.size())) {
        return false;
    }
    if (!writer.writeBlock(reinterpret_cast<const char*>(HatsDataVal.data()), HatsDataVal.size() * sizeof(std::int8_t))) {
        return false;
    }
    if (!writer.writeListEnd()) {
        return false;
    }
    return true;
}

// read (nested) HatsDataVal field
bool AllJoyData::nested_read_HatsDataVal(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    {
        size_t _csize;
        yarp::os::idl::WireState _etype;
        reader.readListBegin(_etype, _csize);
        // WireReader removes BOTTLE_TAG_LIST from the tag
        constexpr int expected_tag = ((BOTTLE_TAG_INT8) & (~BOTTLE_TAG_LIST));
        if constexpr (expected_tag != 0) {
            if (_csize != 0 && _etype.code != expected_tag) {
                return false;
            }
        }
        HatsDataVal.resize(_csize);
        if (_csize != 0 && !reader.readBlock(reinterpret_cast<char*>(HatsDataVal.data()), HatsDataVal.size() * sizeof(std::int8_t))) {
            return false;
        }
        reader.readListEnd();
    }
    return true;
}

// write (nested) HatsDataVal field
bool AllJoyData::nested_write_HatsDataVal(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeListBegin(BOTTLE_TAG_INT8, HatsDataVal.size())) {
        return false;
    }
    if (!writer.writeBlock(reinterpret_cast<const char*>(HatsDataVal.data()), HatsDataVal.size() * sizeof(std::int8_t))) {
        return false;
    }
    if (!writer.writeListEnd()) {
        return false;
    }
    return true;
}

// read TouchDataVal field
bool AllJoyData::read_TouchDataVal(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    {
        size_t _csize;
        yarp::os::idl::WireState _etype;
        reader.readListBegin(_etype, _csize);
        // WireReader removes BOTTLE_TAG_LIST from the tag
        constexpr int expected_tag = ((BOTTLE_TAG_LIST) & (~BOTTLE_TAG_LIST));
        if constexpr (expected_tag != 0) {
            if (_csize != 0 && _etype.code != expected_tag) {
                return false;
            }
        }
        TouchDataVal.resize(_csize);
        for (size_t _i = 0; _i < _csize; ++_i) {
            if (reader.noMore()) {
                reader.fail();
                return false;
            }
            if (!reader.readNested(TouchDataVal[_i])) {
                reader.fail();
                return false;
            }
        }
        reader.readListEnd();
    }
    return true;
}

// write TouchDataVal field
bool AllJoyData::write_TouchDataVal(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeListBegin(BOTTLE_TAG_LIST, TouchDataVal.size())) {
        return false;
    }
    for (const auto& _item : TouchDataVal) {
        if (!writer.writeNested(_item)) {
            return false;
        }
    }
    if (!writer.writeListEnd()) {
        return false;
    }
    return true;
}

// read (nested) TouchDataVal field
bool AllJoyData::nested_read_TouchDataVal(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    {
        size_t _csize;
        yarp::os::idl::WireState _etype;
        reader.readListBegin(_etype, _csize);
        // WireReader removes BOTTLE_TAG_LIST from the tag
        constexpr int expected_tag = ((BOTTLE_TAG_LIST) & (~BOTTLE_TAG_LIST));
        if constexpr (expected_tag != 0) {
            if (_csize != 0 && _etype.code != expected_tag) {
                return false;
            }
        }
        TouchDataVal.resize(_csize);
        for (size_t _i = 0; _i < _csize; ++_i) {
            if (reader.noMore()) {
                reader.fail();
                return false;
            }
            if (!reader.readNested(TouchDataVal[_i])) {
                reader.fail();
                return false;
            }
        }
        reader.readListEnd();
    }
    return true;
}

// write (nested) TouchDataVal field
bool AllJoyData::nested_write_TouchDataVal(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeListBegin(BOTTLE_TAG_LIST, TouchDataVal.size())) {
        return false;
    }
    for (const auto& _item : TouchDataVal) {
        if (!writer.writeNested(_item)) {
            return false;
        }
    }
    if (!writer.writeListEnd()) {
        return false;
    }
    return true;
}

// read StickDataVal field
bool AllJoyData::read_StickDataVal(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    {
        size_t _csize;
        yarp::os::idl::WireState _etype;
        reader.readListBegin(_etype, _csize);
        // WireReader removes BOTTLE_TAG_LIST from the tag
        constexpr int expected_tag = ((BOTTLE_TAG_LIST) & (~BOTTLE_TAG_LIST));
        if constexpr (expected_tag != 0) {
            if (_csize != 0 && _etype.code != expected_tag) {
                return false;
            }
        }
        StickDataVal.resize(_csize);
        for (size_t _i = 0; _i < _csize; ++_i) {
            if (reader.noMore()) {
                reader.fail();
                return false;
            }
            if (!reader.readNested(StickDataVal[_i])) {
                reader.fail();
                return false;
            }
        }
        reader.readListEnd();
    }
    return true;
}

// write StickDataVal field
bool AllJoyData::write_StickDataVal(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeListBegin(BOTTLE_TAG_LIST, StickDataVal.size())) {
        return false;
    }
    for (const auto& _item : StickDataVal) {
        if (!writer.writeNested(_item)) {
            return false;
        }
    }
    if (!writer.writeListEnd()) {
        return false;
    }
    return true;
}

// read (nested) StickDataVal field
bool AllJoyData::nested_read_StickDataVal(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    {
        size_t _csize;
        yarp::os::idl::WireState _etype;
        reader.readListBegin(_etype, _csize);
        // WireReader removes BOTTLE_TAG_LIST from the tag
        constexpr int expected_tag = ((BOTTLE_TAG_LIST) & (~BOTTLE_TAG_LIST));
        if constexpr (expected_tag != 0) {
            if (_csize != 0 && _etype.code != expected_tag) {
                return false;
            }
        }
        StickDataVal.resize(_csize);
        for (size_t _i = 0; _i < _csize; ++_i) {
            if (reader.noMore()) {
                reader.fail();
                return false;
            }
            if (!reader.readNested(StickDataVal[_i])) {
                reader.fail();
                return false;
            }
        }
        reader.readListEnd();
    }
    return true;
}

// write (nested) StickDataVal field
bool AllJoyData::nested_write_StickDataVal(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeListBegin(BOTTLE_TAG_LIST, StickDataVal.size())) {
        return false;
    }
    for (const auto& _item : StickDataVal) {
        if (!writer.writeNested(_item)) {
            return false;
        }
    }
    if (!writer.writeListEnd()) {
        return false;
    }
    return true;
}

// read TrackballDataVal field
bool AllJoyData::read_TrackballDataVal(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    {
        size_t _csize;
        yarp::os::idl::WireState _etype;
        reader.readListBegin(_etype, _csize);
        // WireReader removes BOTTLE_TAG_LIST from the tag
        constexpr int expected_tag = ((BOTTLE_TAG_LIST) & (~BOTTLE_TAG_LIST));
        if constexpr (expected_tag != 0) {
            if (_csize != 0 && _etype.code != expected_tag) {
                return false;
            }
        }
        TrackballDataVal.resize(_csize);
        for (size_t _i = 0; _i < _csize; ++_i) {
            if (reader.noMore()) {
                reader.fail();
                return false;
            }
            if (!reader.readNested(TrackballDataVal[_i])) {
                reader.fail();
                return false;
            }
        }
        reader.readListEnd();
    }
    return true;
}

// write TrackballDataVal field
bool AllJoyData::write_TrackballDataVal(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeListBegin(BOTTLE_TAG_LIST, TrackballDataVal.size())) {
        return false;
    }
    for (const auto& _item : TrackballDataVal) {
        if (!writer.writeNested(_item)) {
            return false;
        }
    }
    if (!writer.writeListEnd()) {
        return false;
    }
    return true;
}

// read (nested) TrackballDataVal field
bool AllJoyData::nested_read_TrackballDataVal(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    {
        size_t _csize;
        yarp::os::idl::WireState _etype;
        reader.readListBegin(_etype, _csize);
        // WireReader removes BOTTLE_TAG_LIST from the tag
        constexpr int expected_tag = ((BOTTLE_TAG_LIST) & (~BOTTLE_TAG_LIST));
        if constexpr (expected_tag != 0) {
            if (_csize != 0 && _etype.code != expected_tag) {
                return false;
            }
        }
        TrackballDataVal.resize(_csize);
        for (size_t _i = 0; _i < _csize; ++_i) {
            if (reader.noMore()) {
                reader.fail();
                return false;
            }
            if (!reader.readNested(TrackballDataVal[_i])) {
                reader.fail();
                return false;
            }
        }
        reader.readListEnd();
    }
    return true;
}

// write (nested) TrackballDataVal field
bool AllJoyData::nested_write_TrackballDataVal(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeListBegin(BOTTLE_TAG_LIST, TrackballDataVal.size())) {
        return false;
    }
    for (const auto& _item : TrackballDataVal) {
        if (!writer.writeNested(_item)) {
            return false;
        }
    }
    if (!writer.writeListEnd()) {
        return false;
    }
    return true;
}

} // namespace yarp::dev
