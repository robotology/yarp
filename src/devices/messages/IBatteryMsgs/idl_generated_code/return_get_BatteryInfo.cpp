/*
 * SPDX-FileCopyrightText: 2006-2021 Istituto Italiano di Tecnologia (IIT)
 * SPDX-License-Identifier: BSD-3-Clause
 */

// Autogenerated by Thrift Compiler (0.14.1-yarped)
//
// This is an automatically generated file.
// It could get re-generated if the ALLOW_IDL_GENERATION flag is on.

#include <return_get_BatteryInfo.h>

// Constructor with field values
return_get_BatteryInfo::return_get_BatteryInfo(const yarp::dev::ReturnValue& result,
                                               const std::string& info) :
        WirePortable(),
        result(result),
        info(info)
{
}

// Read structure on a Wire
bool return_get_BatteryInfo::read(yarp::os::idl::WireReader& reader)
{
    if (!nested_read_result(reader)) {
        return false;
    }
    if (!read_info(reader)) {
        return false;
    }
    if (reader.isError()) {
        return false;
    }
    return true;
}

// Read structure on a Connection
bool return_get_BatteryInfo::read(yarp::os::ConnectionReader& connection)
{
    yarp::os::idl::WireReader reader(connection);
    if (!reader.readListHeader(2)) {
        return false;
    }
    if (!read(reader)) {
        return false;
    }
    return true;
}

// Write structure on a Wire
bool return_get_BatteryInfo::write(const yarp::os::idl::WireWriter& writer) const
{
    if (!nested_write_result(writer)) {
        return false;
    }
    if (!write_info(writer)) {
        return false;
    }
    if (writer.isError()) {
        return false;
    }
    return true;
}

// Write structure on a Connection
bool return_get_BatteryInfo::write(yarp::os::ConnectionWriter& connection) const
{
    yarp::os::idl::WireWriter writer(connection);
    if (!writer.writeListHeader(2)) {
        return false;
    }
    if (!write(writer)) {
        return false;
    }
    return true;
}

// Convert to a printable string
std::string return_get_BatteryInfo::toString() const
{
    yarp::os::Bottle b;
    if (!yarp::os::Portable::copyPortable(*this, b)) {
        return {};
    }
    return b.toString();
}

// read result field
bool return_get_BatteryInfo::read_result(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.read(result)) {
        reader.fail();
        return false;
    }
    return true;
}

// write result field
bool return_get_BatteryInfo::write_result(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.write(result)) {
        return false;
    }
    return true;
}

// read (nested) result field
bool return_get_BatteryInfo::nested_read_result(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readNested(result)) {
        reader.fail();
        return false;
    }
    return true;
}

// write (nested) result field
bool return_get_BatteryInfo::nested_write_result(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeNested(result)) {
        return false;
    }
    return true;
}

// read info field
bool return_get_BatteryInfo::read_info(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readString(info)) {
        reader.fail();
        return false;
    }
    return true;
}

// write info field
bool return_get_BatteryInfo::write_info(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeString(info)) {
        return false;
    }
    return true;
}

// read (nested) info field
bool return_get_BatteryInfo::nested_read_info(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    if (!reader.readString(info)) {
        reader.fail();
        return false;
    }
    return true;
}

// write (nested) info field
bool return_get_BatteryInfo::nested_write_info(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeString(info)) {
        return false;
    }
    return true;
}
