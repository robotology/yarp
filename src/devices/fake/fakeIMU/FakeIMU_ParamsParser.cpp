/*
 * SPDX-FileCopyrightText: 2023-2023 Istituto Italiano di Tecnologia (IIT)
 * SPDX-License-Identifier: LGPL-2.1-or-later
 */


// Generated by yarpDeviceParamParserGenerator (2.0)
// This is an automatically generated file. Please do not edit it.
// It will be re-generated if the cmake flag ALLOW_DEVICE_PARAM_PARSER_GERNERATION is ON.

// Generated on: Thu May 22 11:32:32 2025


#include "FakeIMU_ParamsParser.h"
#include <yarp/os/LogStream.h>
#include <yarp/os/Value.h>

namespace {
    YARP_LOG_COMPONENT(FakeIMUParamsCOMPONENT, "yarp.device.FakeIMU")
}


FakeIMU_ParamsParser::FakeIMU_ParamsParser()
{
}


std::vector<std::string> FakeIMU_ParamsParser::getListOfParams() const
{
    std::vector<std::string> params;
    params.push_back("period");
    params.push_back("constantValue");
    params.push_back("sensorName");
    params.push_back("frameName");
    return params;
}


bool FakeIMU_ParamsParser::getParamValue(const std::string& paramName, std::string& paramValue) const
{
    if (paramName =="period")
    {
        paramValue = std::to_string(m_period);
        return true;
    }
    if (paramName =="constantValue")
    {
        if (m_constantValue==true) paramValue = "true";
        else paramValue = "false";
        return true;
    }
    if (paramName =="sensorName")
    {
        paramValue = m_sensorName;
        return true;
    }
    if (paramName =="frameName")
    {
        paramValue = m_frameName;
        return true;
    }

    yError() <<"parameter '" << paramName << "' was not found";
    return false;

}


std::string FakeIMU_ParamsParser::getConfiguration() const
{
    //This is a sub-optimal solution.
    //Ideally getConfiguration() should return all parameters but it is currently
    //returning only user provided parameters (excluding default values)
    //This behaviour will be fixed in the near future.
    std::string s_cfg = m_provided_configuration;
    return s_cfg;
}

bool      FakeIMU_ParamsParser::parseParams(const yarp::os::Searchable & config)
{
    //Check for --help option
    if (config.check("help"))
    {
        yCInfo(FakeIMUParamsCOMPONENT) << getDocumentationOfDeviceParams();
    }

    m_provided_configuration = config.toString();
    yarp::os::Property prop_check(m_provided_configuration.c_str());
    //Parser of parameter period
    {
        if (config.check("period"))
        {
            m_period = config.find("period").asInt64();
            yCInfo(FakeIMUParamsCOMPONENT) << "Parameter 'period' using value:" << m_period;
        }
        else
        {
            yCInfo(FakeIMUParamsCOMPONENT) << "Parameter 'period' using DEFAULT value:" << m_period;
        }
        prop_check.unput("period");
    }

    //Parser of parameter constantValue
    {
        if (config.check("constantValue"))
        {
            m_constantValue = config.find("constantValue").asBool();
            yCInfo(FakeIMUParamsCOMPONENT) << "Parameter 'constantValue' using value:" << m_constantValue;
        }
        else
        {
            yCInfo(FakeIMUParamsCOMPONENT) << "Parameter 'constantValue' using DEFAULT value:" << m_constantValue;
        }
        prop_check.unput("constantValue");
    }

    //Parser of parameter sensorName
    {
        if (config.check("sensorName"))
        {
            m_sensorName = config.find("sensorName").asString();
            yCInfo(FakeIMUParamsCOMPONENT) << "Parameter 'sensorName' using value:" << m_sensorName;
        }
        else
        {
            yCInfo(FakeIMUParamsCOMPONENT) << "Parameter 'sensorName' using DEFAULT value:" << m_sensorName;
        }
        prop_check.unput("sensorName");
    }

    //Parser of parameter frameName
    {
        if (config.check("frameName"))
        {
            m_frameName = config.find("frameName").asString();
            yCInfo(FakeIMUParamsCOMPONENT) << "Parameter 'frameName' using value:" << m_frameName;
        }
        else
        {
            yCInfo(FakeIMUParamsCOMPONENT) << "Parameter 'frameName' using DEFAULT value:" << m_frameName;
        }
        prop_check.unput("frameName");
    }

    /*
    //This code check if the user set some parameter which are not check by the parser
    //If the parser is set in strict mode, this will generate an error
    if (prop_check.size() > 0)
    {
        bool extra_params_found = false;
        for (auto it=prop_check.begin(); it!=prop_check.end(); it++)
        {
            if (m_parser_is_strict)
            {
                yCError(FakeIMUParamsCOMPONENT) << "User asking for parameter: "<<it->name <<" which is unknown to this parser!";
                extra_params_found = true;
            }
            else
            {
                yCWarning(FakeIMUParamsCOMPONENT) << "User asking for parameter: "<< it->name <<" which is unknown to this parser!";
            }
        }

       if (m_parser_is_strict && extra_params_found)
       {
           return false;
       }
    }
    */
    return true;
}


std::string      FakeIMU_ParamsParser::getDocumentationOfDeviceParams() const
{
    std::string doc;
    doc = doc + std::string("\n=============================================\n");
    doc = doc + std::string("This is the help for device: FakeIMU\n");
    doc = doc + std::string("\n");
    doc = doc + std::string("This is the list of the parameters accepted by the device:\n");
    doc = doc + std::string("'period': Period over which the measurement is updated.\n");
    doc = doc + std::string("'constantValue': If the parameter is present, the fake sensor values never changes (useful for testing server/client coherence).\n");
    doc = doc + std::string("'sensorName': Name of the sensor\n");
    doc = doc + std::string("'frameName': Name of the frame\n");
    doc = doc + std::string("\n");
    doc = doc + std::string("Here are some examples of invocation command with yarpdev, with all params:\n");
    doc = doc + " yarpdev --device fakeIMU --period 10 --constantValue false --sensorName sensorName --frameName frameName\n";
    doc = doc + std::string("Using only mandatory params:\n");
    doc = doc + " yarpdev --device fakeIMU\n";
    doc = doc + std::string("=============================================\n\n");    return doc;
}
