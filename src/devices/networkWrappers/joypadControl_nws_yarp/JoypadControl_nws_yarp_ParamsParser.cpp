/*
 * SPDX-FileCopyrightText: 2023-2023 Istituto Italiano di Tecnologia (IIT)
 * SPDX-License-Identifier: LGPL-2.1-or-later
 */


// Generated by yarpDeviceParamParserGenerator (2.1)
// This is an automatically generated file. Please do not edit it.
// It will be re-generated if the cmake flag ALLOW_DEVICE_PARAM_PARSER_GERNERATION is ON.

// Generated on: Mon May 25 01:27:11 2026


#include "JoypadControl_nws_yarp_ParamsParser.h"
#include <yarp/os/LogStream.h>
#include <yarp/os/Value.h>

namespace {
    YARP_LOG_COMPONENT(JoypadControl_nws_yarpParamsCOMPONENT, "yarp.device.JoypadControl_nws_yarp")
}


JoypadControl_nws_yarp_ParamsParser::JoypadControl_nws_yarp_ParamsParser()
{
}


std::vector<std::string> JoypadControl_nws_yarp_ParamsParser::getListOfParams() const
{
    std::vector<std::string> params;
    params.push_back("period");
    params.push_back("name");
    params.push_back("use_separate_ports");
    params.push_back("profile");
    params.push_back("publish_on_event");
    return params;
}


bool JoypadControl_nws_yarp_ParamsParser::getParamValue(const std::string& paramName, std::string& paramValue) const
{
    if (paramName =="period")
    {
        paramValue = std::to_string(m_period);
        return true;
    }
    if (paramName =="name")
    {
        paramValue = m_name;
        return true;
    }
    if (paramName =="use_separate_ports")
    {
        if (m_use_separate_ports==true) paramValue = "true";
        else paramValue = "false";
        return true;
    }
    if (paramName =="profile")
    {
        if (m_profile==true) paramValue = "true";
        else paramValue = "false";
        return true;
    }
    if (paramName =="publish_on_event")
    {
        if (m_publish_on_event==true) paramValue = "true";
        else paramValue = "false";
        return true;
    }

    yError() <<"parameter '" << paramName << "' was not found";
    return false;

}


std::string JoypadControl_nws_yarp_ParamsParser::getConfiguration() const
{
    //This is a sub-optimal solution.
    //Ideally getConfiguration() should return all parameters but it is currently
    //returning only user provided parameters (excluding default values)
    //This behaviour will be fixed in the near future.
    std::string s_cfg = m_provided_configuration;
    return s_cfg;
}

bool      JoypadControl_nws_yarp_ParamsParser::parseParams(const yarp::os::Searchable & config)
{
    //Check for --help option
    if (config.check("help"))
    {
        yCInfo(JoypadControl_nws_yarpParamsCOMPONENT) << getDocumentationOfDeviceParams();
    }

    m_provided_configuration = config.toString();
    yarp::os::Property prop_check(m_provided_configuration.c_str());
    //Parser of parameter period
    {
        if (config.check("period"))
        {
            m_period = config.find("period").asFloat64();
            yCInfo(JoypadControl_nws_yarpParamsCOMPONENT) << "Parameter 'period' using value:" << m_period;
        }
        else
        {
            yCInfo(JoypadControl_nws_yarpParamsCOMPONENT) << "Parameter 'period' using DEFAULT value:" << m_period;
        }
        prop_check.unput("period");
    }

    //Parser of parameter name
    {
        if (config.check("name"))
        {
            m_name = config.find("name").asString();
            yCInfo(JoypadControl_nws_yarpParamsCOMPONENT) << "Parameter 'name' using value:" << m_name;
        }
        else
        {
            yCError(JoypadControl_nws_yarpParamsCOMPONENT) << "Mandatory parameter 'name' not found!";
            yCError(JoypadControl_nws_yarpParamsCOMPONENT) << "Description of the parameter: Prefix name of the ports opened by the JoypadControlServer";
            return false;
        }
        prop_check.unput("name");
    }

    //Parser of parameter use_separate_ports
    {
        if (config.check("use_separate_ports"))
        {
            m_use_separate_ports = config.find("use_separate_ports").asBool();
            yCInfo(JoypadControl_nws_yarpParamsCOMPONENT) << "Parameter 'use_separate_ports' using value:" << m_use_separate_ports;
        }
        else
        {
            yCInfo(JoypadControl_nws_yarpParamsCOMPONENT) << "Parameter 'use_separate_ports' using DEFAULT value:" << m_use_separate_ports;
        }
        prop_check.unput("use_separate_ports");
    }

    //Parser of parameter profile
    {
        if (config.check("profile"))
        {
            m_profile = config.find("profile").asBool();
            yCInfo(JoypadControl_nws_yarpParamsCOMPONENT) << "Parameter 'profile' using value:" << m_profile;
        }
        else
        {
            yCInfo(JoypadControl_nws_yarpParamsCOMPONENT) << "Parameter 'profile' using DEFAULT value:" << m_profile;
        }
        prop_check.unput("profile");
    }

    //Parser of parameter publish_on_event
    {
        if (config.check("publish_on_event"))
        {
            m_publish_on_event = config.find("publish_on_event").asBool();
            yCInfo(JoypadControl_nws_yarpParamsCOMPONENT) << "Parameter 'publish_on_event' using value:" << m_publish_on_event;
        }
        else
        {
            yCInfo(JoypadControl_nws_yarpParamsCOMPONENT) << "Parameter 'publish_on_event' using DEFAULT value:" << m_publish_on_event;
        }
        prop_check.unput("publish_on_event");
    }

    /*
    //This code check if the user set some parameter which are not check by the parser
    //If the parser is set in strict mode, this will generate an error
    if (prop_check.size() > 0)
    {
        bool extra_params_found = false;
        for (auto it=prop_check.begin(); it!=prop_check.end(); it++)
        {
            if (m_parser_is_strict)
            {
                yCError(JoypadControl_nws_yarpParamsCOMPONENT) << "User asking for parameter: "<<it->name <<" which is unknown to this parser!";
                extra_params_found = true;
            }
            else
            {
                yCWarning(JoypadControl_nws_yarpParamsCOMPONENT) << "User asking for parameter: "<< it->name <<" which is unknown to this parser!";
            }
        }

       if (m_parser_is_strict && extra_params_found)
       {
           return false;
       }
    }
    */
    return true;
}


std::string      JoypadControl_nws_yarp_ParamsParser::getDocumentationOfDeviceParams() const
{
    std::string doc;
    doc = doc + std::string("\n=============================================\n");
    doc = doc + std::string("This is the help for device: JoypadControl_nws_yarp\n");
    doc = doc + std::string("\n");
    doc = doc + std::string("This is the list of the parameters accepted by the device:\n");
    doc = doc + std::string("'period': Refresh period (in s) of the broadcasted values through yarp ports\n");
    doc = doc + std::string("'name': Prefix name of the ports opened by the JoypadControlServer\n");
    doc = doc + std::string("'use_separate_ports': set it to 1 to use separate ports (buttons, axes, trackballs, hats) and 0 to stream all in one single port\n");
    doc = doc + std::string("'profile': print the joypad data for debugging purpose\n");
    doc = doc + std::string("'publish_on_event': if true, events, are published when the status changes (on event). Otherwise they are published at the frequency specified through the period parameter\n");
    doc = doc + std::string("\n");
    doc = doc + std::string("Here are some examples of invocation command with yarpdev, with all params:\n");
    doc = doc + " yarpdev --device JoypadControl_nws_yarp --period 0.01 --name /robotName/joypad --use_separate_ports true --profile false --publish_on_event false\n";
    doc = doc + std::string("Using only mandatory params:\n");
    doc = doc + " yarpdev --device JoypadControl_nws_yarp --name /robotName/joypad\n";
    doc = doc + std::string("=============================================\n\n");    return doc;
}
