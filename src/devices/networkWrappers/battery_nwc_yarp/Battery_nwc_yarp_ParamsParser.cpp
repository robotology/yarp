/*
 * SPDX-FileCopyrightText: 2023-2023 Istituto Italiano di Tecnologia (IIT)
 * SPDX-License-Identifier: LGPL-2.1-or-later
 */


// Generated by yarpDeviceParamParserGenerator (2.1)
// This is an automatically generated file. Please do not edit it.
// It will be re-generated if the cmake flag ALLOW_DEVICE_PARAM_PARSER_GERNERATION is ON.

// Generated on: Thu Apr 23 13:01:45 2026


#include "Battery_nwc_yarp_ParamsParser.h"
#include <yarp/os/LogStream.h>
#include <yarp/os/Value.h>

namespace {
    YARP_LOG_COMPONENT(Battery_nwc_yarpParamsCOMPONENT, "yarp.device.Battery_nwc_yarp")
}


Battery_nwc_yarp_ParamsParser::Battery_nwc_yarp_ParamsParser()
{
}


std::vector<std::string> Battery_nwc_yarp_ParamsParser::getListOfParams() const
{
    std::vector<std::string> params;
    params.push_back("local");
    params.push_back("remote");
    params.push_back("rpc_carrier");
    params.push_back("rpc_timeout");
    params.push_back("carrier");
    return params;
}


bool Battery_nwc_yarp_ParamsParser::getParamValue(const std::string& paramName, std::string& paramValue) const
{
    if (paramName =="local")
    {
        paramValue = m_local;
        return true;
    }
    if (paramName =="remote")
    {
        paramValue = m_remote;
        return true;
    }
    if (paramName =="rpc_carrier")
    {
        paramValue = m_rpc_carrier;
        return true;
    }
    if (paramName =="rpc_timeout")
    {
        paramValue = std::to_string(m_rpc_timeout);
        return true;
    }
    if (paramName =="carrier")
    {
        paramValue = m_carrier;
        return true;
    }

    yError() <<"parameter '" << paramName << "' was not found";
    return false;

}


std::string Battery_nwc_yarp_ParamsParser::getConfiguration() const
{
    //This is a sub-optimal solution.
    //Ideally getConfiguration() should return all parameters but it is currently
    //returning only user provided parameters (excluding default values)
    //This behaviour will be fixed in the near future.
    std::string s_cfg = m_provided_configuration;
    return s_cfg;
}

bool      Battery_nwc_yarp_ParamsParser::parseParams(const yarp::os::Searchable & config)
{
    //Check for --help option
    if (config.check("help"))
    {
        yCInfo(Battery_nwc_yarpParamsCOMPONENT) << getDocumentationOfDeviceParams();
    }

    m_provided_configuration = config.toString();
    yarp::os::Property prop_check(m_provided_configuration.c_str());
    //Parser of parameter local
    {
        if (config.check("local"))
        {
            m_local = config.find("local").asString();
            yCInfo(Battery_nwc_yarpParamsCOMPONENT) << "Parameter 'local' using value:" << m_local;
        }
        else
        {
            yCError(Battery_nwc_yarpParamsCOMPONENT) << "Mandatory parameter 'local' not found!";
            yCError(Battery_nwc_yarpParamsCOMPONENT) << "Description of the parameter: Full port name opened by the nwc device.";
            return false;
        }
        prop_check.unput("local");
    }

    //Parser of parameter remote
    {
        if (config.check("remote"))
        {
            m_remote = config.find("remote").asString();
            yCInfo(Battery_nwc_yarpParamsCOMPONENT) << "Parameter 'remote' using value:" << m_remote;
        }
        else
        {
            yCError(Battery_nwc_yarpParamsCOMPONENT) << "Mandatory parameter 'remote' not found!";
            yCError(Battery_nwc_yarpParamsCOMPONENT) << "Description of the parameter: Full port name of the port remotely opened by the nws device, to which the nwc connects to.";
            return false;
        }
        prop_check.unput("remote");
    }

    //Parser of parameter rpc_carrier
    {
        if (config.check("rpc_carrier"))
        {
            m_rpc_carrier = config.find("rpc_carrier").asString();
            yCInfo(Battery_nwc_yarpParamsCOMPONENT) << "Parameter 'rpc_carrier' using value:" << m_rpc_carrier;
        }
        else
        {
            yCInfo(Battery_nwc_yarpParamsCOMPONENT) << "Parameter 'rpc_carrier' using DEFAULT value:" << m_rpc_carrier;
        }
        prop_check.unput("rpc_carrier");
    }

    //Parser of parameter rpc_timeout
    {
        if (config.check("rpc_timeout"))
        {
            m_rpc_timeout = config.find("rpc_timeout").asFloat64();
            yCInfo(Battery_nwc_yarpParamsCOMPONENT) << "Parameter 'rpc_timeout' using value:" << m_rpc_timeout;
        }
        else
        {
            yCInfo(Battery_nwc_yarpParamsCOMPONENT) << "Parameter 'rpc_timeout' using DEFAULT value:" << m_rpc_timeout;
        }
        prop_check.unput("rpc_timeout");
    }

    //Parser of parameter carrier
    {
        if (config.check("carrier"))
        {
            m_carrier = config.find("carrier").asString();
            yCInfo(Battery_nwc_yarpParamsCOMPONENT) << "Parameter 'carrier' using value:" << m_carrier;
        }
        else
        {
            yCInfo(Battery_nwc_yarpParamsCOMPONENT) << "Parameter 'carrier' using DEFAULT value:" << m_carrier;
        }
        prop_check.unput("carrier");
    }

    /*
    //This code check if the user set some parameter which are not check by the parser
    //If the parser is set in strict mode, this will generate an error
    if (prop_check.size() > 0)
    {
        bool extra_params_found = false;
        for (auto it=prop_check.begin(); it!=prop_check.end(); it++)
        {
            if (m_parser_is_strict)
            {
                yCError(Battery_nwc_yarpParamsCOMPONENT) << "User asking for parameter: "<<it->name <<" which is unknown to this parser!";
                extra_params_found = true;
            }
            else
            {
                yCWarning(Battery_nwc_yarpParamsCOMPONENT) << "User asking for parameter: "<< it->name <<" which is unknown to this parser!";
            }
        }

       if (m_parser_is_strict && extra_params_found)
       {
           return false;
       }
    }
    */
    return true;
}


std::string      Battery_nwc_yarp_ParamsParser::getDocumentationOfDeviceParams() const
{
    std::string doc;
    doc = doc + std::string("\n=============================================\n");
    doc = doc + std::string("This is the help for device: Battery_nwc_yarp\n");
    doc = doc + std::string("\n");
    doc = doc + std::string("This is the list of the parameters accepted by the device:\n");
    doc = doc + std::string("'local': Full port name opened by the nwc device.\n");
    doc = doc + std::string("'remote': Full port name of the port remotely opened by the nws device, to which the nwc connects to.\n");
    doc = doc + std::string("'rpc_carrier': The carrier used for the RPC connection with the server.\n");
    doc = doc + std::string("'rpc_timeout': Timeout for RPC calls. Default value: -1 = infinite\n");
    doc = doc + std::string("'carrier': The carrier used for the streaming connection with the server.\n");
    doc = doc + std::string("\n");
    doc = doc + std::string("Here are some examples of invocation command with yarpdev, with all params:\n");
    doc = doc + " yarpdev --device battery_nwc_yarp --local <mandatory_value> --remote <mandatory_value> --rpc_carrier fast_tcp --rpc_timeout -1 --carrier fast_tcp\n";
    doc = doc + std::string("Using only mandatory params:\n");
    doc = doc + " yarpdev --device battery_nwc_yarp --local <mandatory_value> --remote <mandatory_value>\n";
    doc = doc + std::string("=============================================\n\n");    return doc;
}
