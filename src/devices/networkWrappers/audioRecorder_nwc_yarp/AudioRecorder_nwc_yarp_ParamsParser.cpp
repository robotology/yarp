/*
 * SPDX-FileCopyrightText: 2023-2023 Istituto Italiano di Tecnologia (IIT)
 * SPDX-License-Identifier: LGPL-2.1-or-later
 */


// Generated by yarpDeviceParamParserGenerator (2.0)
// This is an automatically generated file. Please do not edit it.
// It will be re-generated if the cmake flag ALLOW_DEVICE_PARAM_PARSER_GERNERATION is ON.

// Generated on: Thu May 22 11:32:40 2025


#include "AudioRecorder_nwc_yarp_ParamsParser.h"
#include <yarp/os/LogStream.h>
#include <yarp/os/Value.h>

namespace {
    YARP_LOG_COMPONENT(AudioRecorder_nwc_yarpParamsCOMPONENT, "yarp.device.AudioRecorder_nwc_yarp")
}


AudioRecorder_nwc_yarp_ParamsParser::AudioRecorder_nwc_yarp_ParamsParser()
{
}


std::vector<std::string> AudioRecorder_nwc_yarp_ParamsParser::getListOfParams() const
{
    std::vector<std::string> params;
    params.push_back("local");
    params.push_back("remote");
    params.push_back("carrier");
    params.push_back("useStream");
    return params;
}


bool AudioRecorder_nwc_yarp_ParamsParser::getParamValue(const std::string& paramName, std::string& paramValue) const
{
    if (paramName =="local")
    {
        paramValue = m_local;
        return true;
    }
    if (paramName =="remote")
    {
        paramValue = m_remote;
        return true;
    }
    if (paramName =="carrier")
    {
        paramValue = m_carrier;
        return true;
    }
    if (paramName =="useStream")
    {
        if (m_useStream==true) paramValue = "true";
        else paramValue = "false";
        return true;
    }

    yError() <<"parameter '" << paramName << "' was not found";
    return false;

}


std::string AudioRecorder_nwc_yarp_ParamsParser::getConfiguration() const
{
    //This is a sub-optimal solution.
    //Ideally getConfiguration() should return all parameters but it is currently
    //returning only user provided parameters (excluding default values)
    //This behaviour will be fixed in the near future.
    std::string s_cfg = m_provided_configuration;
    return s_cfg;
}

bool      AudioRecorder_nwc_yarp_ParamsParser::parseParams(const yarp::os::Searchable & config)
{
    //Check for --help option
    if (config.check("help"))
    {
        yCInfo(AudioRecorder_nwc_yarpParamsCOMPONENT) << getDocumentationOfDeviceParams();
    }

    m_provided_configuration = config.toString();
    yarp::os::Property prop_check(m_provided_configuration.c_str());
    //Parser of parameter local
    {
        if (config.check("local"))
        {
            m_local = config.find("local").asString();
            yCInfo(AudioRecorder_nwc_yarpParamsCOMPONENT) << "Parameter 'local' using value:" << m_local;
        }
        else
        {
            yCError(AudioRecorder_nwc_yarpParamsCOMPONENT) << "Mandatory parameter 'local' not found!";
            yCError(AudioRecorder_nwc_yarpParamsCOMPONENT) << "Description of the parameter: Full port name opened by the audioRecoder_nwc_yarp device.";
            return false;
        }
        prop_check.unput("local");
    }

    //Parser of parameter remote
    {
        if (config.check("remote"))
        {
            m_remote = config.find("remote").asString();
            yCInfo(AudioRecorder_nwc_yarpParamsCOMPONENT) << "Parameter 'remote' using value:" << m_remote;
        }
        else
        {
            yCError(AudioRecorder_nwc_yarpParamsCOMPONENT) << "Mandatory parameter 'remote' not found!";
            yCError(AudioRecorder_nwc_yarpParamsCOMPONENT) << "Description of the parameter: Full port name of the port opened on the server side, to which the audioRecoder_nwc_yarp connects to.";
            return false;
        }
        prop_check.unput("remote");
    }

    //Parser of parameter carrier
    {
        if (config.check("carrier"))
        {
            m_carrier = config.find("carrier").asString();
            yCInfo(AudioRecorder_nwc_yarpParamsCOMPONENT) << "Parameter 'carrier' using value:" << m_carrier;
        }
        else
        {
            yCInfo(AudioRecorder_nwc_yarpParamsCOMPONENT) << "Parameter 'carrier' using DEFAULT value:" << m_carrier;
        }
        prop_check.unput("carrier");
    }

    //Parser of parameter useStream
    {
        if (config.check("useStream"))
        {
            m_useStream = config.find("useStream").asBool();
            yCInfo(AudioRecorder_nwc_yarpParamsCOMPONENT) << "Parameter 'useStream' using value:" << m_useStream;
        }
        else
        {
            yCInfo(AudioRecorder_nwc_yarpParamsCOMPONENT) << "Parameter 'useStream' using DEFAULT value:" << m_useStream;
        }
        prop_check.unput("useStream");
    }

    /*
    //This code check if the user set some parameter which are not check by the parser
    //If the parser is set in strict mode, this will generate an error
    if (prop_check.size() > 0)
    {
        bool extra_params_found = false;
        for (auto it=prop_check.begin(); it!=prop_check.end(); it++)
        {
            if (m_parser_is_strict)
            {
                yCError(AudioRecorder_nwc_yarpParamsCOMPONENT) << "User asking for parameter: "<<it->name <<" which is unknown to this parser!";
                extra_params_found = true;
            }
            else
            {
                yCWarning(AudioRecorder_nwc_yarpParamsCOMPONENT) << "User asking for parameter: "<< it->name <<" which is unknown to this parser!";
            }
        }

       if (m_parser_is_strict && extra_params_found)
       {
           return false;
       }
    }
    */
    return true;
}


std::string      AudioRecorder_nwc_yarp_ParamsParser::getDocumentationOfDeviceParams() const
{
    std::string doc;
    doc = doc + std::string("\n=============================================\n");
    doc = doc + std::string("This is the help for device: AudioRecorder_nwc_yarp\n");
    doc = doc + std::string("\n");
    doc = doc + std::string("This is the list of the parameters accepted by the device:\n");
    doc = doc + std::string("'local': Full port name opened by the audioRecoder_nwc_yarp device.\n");
    doc = doc + std::string("'remote': Full port name of the port opened on the server side, to which the audioRecoder_nwc_yarp connects to.\n");
    doc = doc + std::string("'carrier': The carrier used for the streaming connection with the server.\n");
    doc = doc + std::string("'useStream': If true, enables a streaming connection with the server. Otherwise only rpc is used\n");
    doc = doc + std::string("\n");
    doc = doc + std::string("Here are some examples of invocation command with yarpdev, with all params:\n");
    doc = doc + " yarpdev --device audioRecorder_nwc_yarp --local <mandatory_value> --remote <mandatory_value> --carrier fast_tcp --useStream false\n";
    doc = doc + std::string("Using only mandatory params:\n");
    doc = doc + " yarpdev --device audioRecorder_nwc_yarp --local <mandatory_value> --remote <mandatory_value>\n";
    doc = doc + std::string("=============================================\n\n");    return doc;
}
