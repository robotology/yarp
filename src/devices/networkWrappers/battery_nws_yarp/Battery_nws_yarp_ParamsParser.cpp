/*
 * SPDX-FileCopyrightText: 2023-2023 Istituto Italiano di Tecnologia (IIT)
 * SPDX-License-Identifier: LGPL-2.1-or-later
 */


// Generated by yarpDeviceParamParserGenerator (1.0)
// This is an automatically generated file. Please do not edit it.
// It will be re-generated if the cmake flag ALLOW_DEVICE_PARAM_PARSER_GERNERATION is ON.

// Generated on: Wed May 21 11:58:38 2025


#include "Battery_nws_yarp_ParamsParser.h"
#include <yarp/os/LogStream.h>
#include <yarp/os/Value.h>

namespace {
    YARP_LOG_COMPONENT(Battery_nws_yarpParamsCOMPONENT, "yarp.device.Battery_nws_yarp")
}


Battery_nws_yarp_ParamsParser::Battery_nws_yarp_ParamsParser()
{
}


std::vector<std::string> Battery_nws_yarp_ParamsParser::getListOfParams() const
{
    std::vector<std::string> params;
    params.push_back("name");
    params.push_back("period");
    params.push_back("enable_shutdown");
    params.push_back("enable_log");
    params.push_back("quitPortName");
    return params;
}


bool Battery_nws_yarp_ParamsParser::getParamValue(const std::string& paramName, std::string& paramValue) const
{
    if (paramName =="name")
    {
        paramValue = m_name;
        return true;
    }
    if (paramName =="period")
    {
        paramValue = std::to_string(m_period);
        return true;
    }
    if (paramName =="enable_shutdown")
    {
        if (m_enable_shutdown==true) paramValue = "true";
        else paramValue = "false";
        return true;
    }
    if (paramName =="enable_log")
    {
        if (m_enable_log==true) paramValue = "true";
        else paramValue = "false";
        return true;
    }
    if (paramName =="quitPortName")
    {
        paramValue = m_quitPortName;
        return true;
    }

    yError() <<"parameter '" << paramName << "' was not found";
    return false;

}


bool      Battery_nws_yarp_ParamsParser::parseParams(const yarp::os::Searchable & config)
{
    //Check for --help option
    if (config.check("help"))
    {
        yCInfo(Battery_nws_yarpParamsCOMPONENT) << getDocumentationOfDeviceParams();
    }

    std::string config_string = config.toString();
    yarp::os::Property prop_check(config_string.c_str());
    //Parser of parameter name
    {
        if (config.check("name"))
        {
            m_name = config.find("name").asString();
            yCInfo(Battery_nws_yarpParamsCOMPONENT) << "Parameter 'name' using value:" << m_name;
        }
        else
        {
            yCError(Battery_nws_yarpParamsCOMPONENT) << "Mandatory parameter 'name' not found!";
            yCError(Battery_nws_yarpParamsCOMPONENT) << "Description of the parameter: prefix of the ports opened by the device";
            return false;
        }
        prop_check.unput("name");
    }

    //Parser of parameter period
    {
        if (config.check("period"))
        {
            m_period = config.find("period").asFloat64();
            yCInfo(Battery_nws_yarpParamsCOMPONENT) << "Parameter 'period' using value:" << m_period;
        }
        else
        {
            yCInfo(Battery_nws_yarpParamsCOMPONENT) << "Parameter 'period' using DEFAULT value:" << m_period;
        }
        prop_check.unput("period");
    }

    //Parser of parameter enable_shutdown
    {
        if (config.check("enable_shutdown"))
        {
            m_enable_shutdown = config.find("enable_shutdown").asBool();
            yCInfo(Battery_nws_yarpParamsCOMPONENT) << "Parameter 'enable_shutdown' using value:" << m_enable_shutdown;
        }
        else
        {
            yCInfo(Battery_nws_yarpParamsCOMPONENT) << "Parameter 'enable_shutdown' using DEFAULT value:" << m_enable_shutdown;
        }
        prop_check.unput("enable_shutdown");
    }

    //Parser of parameter enable_log
    {
        if (config.check("enable_log"))
        {
            m_enable_log = config.find("enable_log").asBool();
            yCInfo(Battery_nws_yarpParamsCOMPONENT) << "Parameter 'enable_log' using value:" << m_enable_log;
        }
        else
        {
            yCInfo(Battery_nws_yarpParamsCOMPONENT) << "Parameter 'enable_log' using DEFAULT value:" << m_enable_log;
        }
        prop_check.unput("enable_log");
    }

    //Parser of parameter quitPortName
    {
        if (config.check("quitPortName"))
        {
            m_quitPortName = config.find("quitPortName").asString();
            yCInfo(Battery_nws_yarpParamsCOMPONENT) << "Parameter 'quitPortName' using value:" << m_quitPortName;
        }
        else
        {
            yCInfo(Battery_nws_yarpParamsCOMPONENT) << "Parameter 'quitPortName' using DEFAULT value:" << m_quitPortName;
        }
        prop_check.unput("quitPortName");
    }

    /*
    //This code check if the user set some parameter which are not check by the parser
    //If the parser is set in strict mode, this will generate an error
    if (prop_check.size() > 0)
    {
        bool extra_params_found = false;
        for (auto it=prop_check.begin(); it!=prop_check.end(); it++)
        {
            if (m_parser_is_strict)
            {
                yCError(Battery_nws_yarpParamsCOMPONENT) << "User asking for parameter: "<<it->name <<" which is unknown to this parser!";
                extra_params_found = true;
            }
            else
            {
                yCWarning(Battery_nws_yarpParamsCOMPONENT) << "User asking for parameter: "<< it->name <<" which is unknown to this parser!";
            }
        }

       if (m_parser_is_strict && extra_params_found)
       {
           return false;
       }
    }
    */
    return true;
}


std::string      Battery_nws_yarp_ParamsParser::getDocumentationOfDeviceParams() const
{
    std::string doc;
    doc = doc + std::string("\n=============================================\n");
    doc = doc + std::string("This is the help for device: Battery_nws_yarp\n");
    doc = doc + std::string("\n");
    doc = doc + std::string("This is the list of the parameters accepted by the device:\n");
    doc = doc + std::string("'name': prefix of the ports opened by the device\n");
    doc = doc + std::string("'period': refresh period of the broadcasted values in seconds\n");
    doc = doc + std::string("'enable_shutdown': if enabled, batteryWrapper will start a system shutdown when charge is below 5%\n");
    doc = doc + std::string("'enable_log': if enabled, stores a log of battery usage on disk\n");
    doc = doc + std::string("'quitPortName': if present, this yarp port will be opened and used to send a sthutdown command to other processes\n");
    doc = doc + std::string("\n");
    doc = doc + std::string("Here are some examples of invocation command with yarpdev, with all params:\n");
    doc = doc + " yarpdev --device battery_nws_yarp --name /robotName/battery1 --period 1.0 --enable_shutdown false --enable_log false --quitPortName <optional_value>\n";
    doc = doc + std::string("Using only mandatory params:\n");
    doc = doc + " yarpdev --device battery_nws_yarp --name /robotName/battery1\n";
    doc = doc + std::string("=============================================\n\n");    return doc;
}
